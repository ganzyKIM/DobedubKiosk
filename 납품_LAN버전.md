# 납품용 중간 버전 — v2.4 (같은 망 관리판)

**브랜치 `release/lan-v2.4` / 태그 `v2.4-lan` / `versionCode=25` / `versionName=2.4`**

회사 납품용으로 고정한 중간 저장본이다. **개발선(`mac-dev`)과 갈라져 있으며, 이쪽으로
원격 관리(NetBird) 작업은 들어오지 않는다.** 개발선의 최신 상태가 필요하면 그쪽을 볼 것.

## 이 버전에 들어 있는 것

관리자 PC와 태블릿이 **같은 와이파이**에 있을 때 동작하는 함대 관리 한 세트:

- **동영상 플레이어 아동용 UI** — 재생/일시정지 88dp 원형 버튼, ±5초·처음부터 64dp,
  상단 '목록' 알약. 홈 화면과 같은 Tabler 아이콘 체계로 통일 (이 버전의 마지막 추가분)
- **영상 자료실 + 기기별 배포** — 대시보드에서 체크박스로 일괄 전송, 원격 삭제
- **영상별 썸네일 관리** — 자료실에서 이미지 등록 → 보유 기기에만 자동 배포.
  **교체하면 태블릿 목록 화면이 열려 있어도 10초 안에 갱신된다**
- **전송 진행률** — 태블릿 화면의 "받는 중" 카드, 대시보드의 퍼센트 칩(2초 폴링)
- **즉시 지시(long-poll)** — 관리자가 누르면 몇 초 안에 태블릿이 반응
- **기기별 이용안내 이미지** — 낱장/전체 교체, 순서 지정
- **앱 자동 업데이트** — 홈 화면 유휴 시 무인 설치, 알림 보내기, 배포 이력·롤백
- **원격 설정** — 문의 연락처, 관리자 PIN 초기화
- **서버 자동 찾기** — 같은 와이파이의 서브넷을 훑어 주소 자동 입력(주소 축약 입력도 지원)

## 이 버전에서 **빠진** 것 (의도적)

- **다른 망(도서관)에 나간 태블릿 원격 관리** — NetBird VPN 연동 전체.
  태블릿이 관리자 PC와 같은 와이파이에 없으면 관리되지 않는다
- 원격 재부팅 / 서버 주소 원격 변경 / always-on VPN 지정
- 그에 딸린 대시보드 버튼과 "VPN 미지정" 경고

이 기능들은 개발선(`mac-dev`, v2.5.2~)에 있고, 검증이 끝나는 대로 별도 납품한다.

## 재빌드 방법

```bash
git checkout v2.4-lan
```

`keystore.properties` + `release-keystore.jks` 를 저장소 루트에 두고(**git 밖 별도 보관**),
`local.properties` 에 `sdk.dir` 을 적은 뒤:

```bash
./gradlew testDebugUnitTest assembleRelease
```

산출물: `app/build/outputs/apk/release/app-release.apk` (code 25, v2.4)

> ⚠ **서명 키를 잃으면 이미 배포된 태블릿을 다시는 업데이트할 수 없다.** 기기마다
> 공장초기화 + 재설치가 필요해진다. 이 납품본의 서명 인증서 SHA-256 은
> `79416e56 4a001819 1d6d2907 1a094c51 88bbab0c a508042d 48b9b75c 576d0389` —
> 이후 어떤 버전을 올리든 이 값이 같아야 한다.

## 윈도우 관리자 PC에서 처음 돌릴 때 (순서대로)

1. **Node.js 22 LTS 설치** — <https://nodejs.org/> (서버 실행에 필요)
2. **`서버-준비.bat` 더블클릭** — 서버 의존성을 내려받는다. **최초 1회, 인터넷 필요.**
   - 의존성에 네이티브 모듈(better-sqlite3)이 들어 있어 **설치는 반드시 서버를 돌릴
     그 PC에서** 해야 한다. 다른 OS에서 만든 `node_modules` 를 복사해 오면 동작하지 않는다
     (그래서 이 꾸러미에는 일부러 넣지 않았다).
3. **`관리자-실행.bat` 더블클릭** — 함대 서버가 뜨고 대시보드가 열린다.
   비밀번호는 `server/data/admin-password.txt` 에 자동 생성된 값.
   - **PC를 재부팅하면 자동으로 뜨지 않는다.** 다시 `관리자-실행.bat` 을 누를 것
4. 태블릿 세팅은 `태블릿-세팅.bat` — 별도로 **Android platform-tools(adb)** 가 필요하다.
   PATH·기존 SDK·스크립트 옆 `platform-tools\` 순으로 찾는다. 상세는 `납품_매뉴얼.md`
5. 태블릿 관리자 화면에서 **"이 와이파이에서 서버 자동 찾기"** 로 서버 주소를 넣는다
   (관리자 PC와 **같은 와이파이**여야 한다 — 이 버전의 전제)

운영 화면 사용법은 `PC관리자_사용법.md`, 서버 API 상세는 `server/README.md`.
서버 테스트: `cd server && npm test` (12개).

> 배치 파일은 CRLF 줄바꿈으로 고정돼 있다(`.gitattributes`). 맥/리눅스에서 이 꾸러미를
> 다시 만들 때도 그대로 유지할 것 — LF 로 풀리면 `cmd.exe` 가 괄호 블록을 잘못 읽는다.

## 태블릿 세팅

`태블릿-세팅.bat` 더블클릭 → 안내대로. 상세는 `납품_매뉴얼.md`.
서버 주소는 관리자 화면(제목 5회 탭 → PIN)의 **"이 와이파이에서 서버 자동 찾기"** 로 넣는다.

## 버전 번호 규칙

`versionCode` 는 **절대 되돌리지 않는다**. 이 납품선에서 수정본이 필요하면 26 이상이
개발선과 겹치므로, **납품선 패치는 `versionName` 을 `2.4.x` 로 두되 `versionCode` 는
개발선 최신값보다 큰 수를 쓴다**(현재 개발선 최고값 29 → 납품선 패치는 30부터).
같은 태블릿에 두 계열을 섞어 올리지 않는 것이 원칙이다.
