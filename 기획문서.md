# 두비덥 도서관 태블릿 키오스크 — 개발 기획 문서

- 문서 버전: v0.3 (2026-07-12)
- 작성 목적: 도서관 납품용 태블릿 키오스크 앱 개발을 위한 기획 기준 문서
- 범위: 태블릿 단말 앱(키오스크 셸 + 3개 기능). **백오피스는 후속 범위** — 본 문서에서는 연동을 위한 확장 포인트만 정의한다.
- **구현체**: 이 저장소(`DobedubKiosk/`) — Phase 1~4 전체 구현 및 빌드/단위테스트 검증 완료. 사용법은 [README.md](README.md) 참고.

---

## 1. 개요

| 항목 | 내용 |
|---|---|
| 프로젝트명 | 두비덥 도서관 태블릿 키오스크 (가칭) |
| 목적 | 도서관에 납품하는 태블릿을 식당/카페 키오스크처럼 완전 잠금하고, 지정된 3개 기능만 제공 |
| 대상 기기 | Lenovo Tab P11 (모델명 TB-J606F) |
| 사용자 | 도서관 방문객(어린이 포함), 도서관 사서/관리자, 두비덥 운영자 |
| 핵심 요구 | ① 단말의 다른 기능·화면 접근 완전 차단 ② 관리자 / 동영상 플레이어 / 제한된 웹사이트 접근 3개 기능만 제공 |

### 1.1 대상 기기 스펙 (Lenovo TB-J606F)

| 항목 | 스펙 | 개발 시사점 |
|---|---|---|
| 화면 | 11인치 IPS, **2000×1200 (WUXGA)**, 가로 사용 기준 | 가로(landscape) 고정 레이아웃 설계. IPS라 번인 부담 낮음 |
| AP / RAM | Snapdragon 662 / 4~6GB | 중급 성능 — 1080p H.264 하드웨어 디코딩 무난, 4K는 지양 |
| OS | Android 10 출고, **Android 11~12 업그레이드 지원** | minSdk 29(Android 10) 기준 개발, Device Owner API 전부 사용 가능 |
| 저장소 | 64/128GB + microSD | 동영상 로컬 저장 충분. 콘텐츠는 내부 저장소 앱 전용 폴더 사용 권장 |
| 기타 | 쿼드 스피커(Dolby Atmos), USB-C, Wi-Fi 전용(J606F) | 셀룰러 없음 → 네트워크는 도서관 Wi-Fi 의존 |
| 버튼 | 전원, 볼륨(물리), 홈버튼 없음(소프트 내비게이션) | 소프트 내비바는 Lock Task로 차단 가능. 전원 버튼은 차단 불가(§9 참고) |

---

## 2. 키오스크 잠금 아키텍처 (핵심)

### 2.1 방식 비교 및 결정

| 방식 | 잠금 강도 | 비고 |
|---|---|---|
| **A. Device Owner + Lock Task Mode (채택)** | 완전 잠금 | 홈/최근 앱/상태바/알림/전원 메뉴까지 제어. 사용자 해제 불가 |
| B. Screen Pinning(화면 고정) | 약함 | 사용자가 버튼 조합으로 해제 가능 → 납품용 부적합 |
| C. 상용 MDM/키오스크 솔루션 | 강함 | 라이선스 비용 발생, 자체 백오피스 계획과 중복 → 제외 |

**결정: 자체 앱을 Device Owner로 지정 + 커스텀 런처(HOME) + Lock Task Mode 3중 구조.**

### 2.2 구현 요소

1. **Device Owner 프로비저닝**
   - 공장초기화 직후(계정 추가 전) 상태에서 `adb shell dpm set-device-owner com.dobedub.kiosk/.AdminReceiver` 실행 또는 QR 코드 프로비저닝.
   - 납품 준비 절차(§10 체크리스트)에 포함. **한 번 계정이 추가된 기기는 다시 공장초기화해야 함.**
2. **커스텀 런처(HOME 역할)**
   - 앱 매니페스트에 `HOME`/`DEFAULT` 카테고리 등록, `setPersistentPreferredActivity()`로 홈 버튼·재부팅 시 항상 본 앱 진입.
   - 앱 크래시 시에도 시스템이 홈(=본 앱)을 재기동 → 자체 복구.
3. **Lock Task Mode**
   - `setLockTaskPackages()` + `startLockTask()`로 태스크 고정.
   - `setLockTaskFeatures()`로 상태바·알림·홈·최근앱·전원 메뉴 노출 제어.
4. **추가 시스템 잠금 (DevicePolicyManager User Restrictions)**
   - `DISALLOW_SAFE_BOOT` — 안전 모드 부팅 차단
   - `DISALLOW_FACTORY_RESET` — 사용자 공장초기화 차단
   - `DISALLOW_DEBUGGING_FEATURES` — USB 디버깅 차단
   - `DISALLOW_ADD_USER`, `DISALLOW_INSTALL_APPS`, `DISALLOW_USB_FILE_TRANSFER` 등
   - `setStatusBarDisabled(true)` — 상태바 끌어내리기 차단
   - `setKeyguardDisabled(true)` — 잠금화면 제거(전원 버튼 후 바로 키오스크 복귀)
5. **상시 구동 정책**
   - `BOOT_COMPLETED` 리시버 + persistent preferred HOME → 재부팅 시 자동 키오스크 진입.
   - `FLAG_KEEP_SCREEN_ON` + 관리자 설정의 절전 스케줄(운영시간 외 화면 어둡게).
   - 미디어 볼륨 외 조작 무의미하도록 볼륨 정책 적용(관리자 설정에서 상한 지정).

### 2.3 일반 상태 복귀 (관리자 기능)

- 관리자 메뉴에서 "키오스크 해제" 실행 시: `stopLockTask()` → user restriction 해제 → HOME preferred 해제 → 기본 런처 노출.
- Device Owner 자체 해제(완전 반납 시): `clearDeviceOwnerApp()` 제공 — 2단계 확인 후 실행.

---

## 3. 정보 구조(IA) 및 화면 흐름

```
[부팅] ─→ [홈 화면 (키오스크 런처)]
              ├─ 대형 타일 ①: 동영상 보기 ─→ [동영상 목록] ─→ [전체화면 재생]
              ├─ 대형 타일 ②: 웹사이트   ─→ [제한 웹뷰]
              └─ (숨김) 관리자 진입 ─→ [PIN 입력] ─→ [관리자 설정]
```

- **홈 화면**: 상단에 두비덥 로고 헤더(피그마 헤더 스타일 준용), 중앙에 2개의 대형 터치 타일(동영상 / 웹사이트). 어린이도 조작 가능하도록 타일 최소 터치 영역 크게(화면의 1/3 이상).
- **관리자 진입(숨김)**: 홈 화면 로고를 **연속 5회 탭** → PIN 입력 다이얼로그. (도서관 이용자가 우연히 진입하지 못하도록 UI상 노출 없음)
- **무조작 복귀**: 동영상 목록·웹뷰에서 N분(기본 5분, 관리자 설정) 무조작 시 홈 화면으로 자동 복귀.

---

## 4. 기능 상세

### 4.1 관리자

| 구분 | 내용 |
|---|---|
| 진입 | 홈 화면 로고 5회 연속 탭 → PIN 입력(숫자 4~6자리) |
| 인증 | PIN은 해시(salt 포함) 저장. 5회 연속 실패 시 30초 잠금(이후 실패마다 지연 증가) |
| 초기 PIN | 납품 시 기기별 지정, 최초 진입 시 변경 유도 |

**관리자 메뉴 구성**

1. **키오스크 관리**
   - 키오스크 모드 해제(일반 태블릿 상태로 복귀) / 재진입
   - 기기 재부팅
   - Device Owner 완전 해제(반납용, 2단계 확인)
2. **관리자 정보**
   - PIN 변경
   - 관리 연락처(문제 발생 시 표시할 안내 문구) 편집
3. **콘텐츠 설정**
   - 동영상 폴더 경로 확인, 목록 새로고침, 개별 파일 삭제
   - 웹사이트 시작 URL 및 **허용 도메인 목록** 편집(추가/삭제)
4. **기기 설정**
   - 화면 밝기, 미디어 볼륨(상한), 무조작 복귀 시간(분)
   - 운영 시간 스케줄(예: 09:00~18:00 외 화면 절전) — 2차 범위 가능
5. **정보**
   - 앱 버전, 기기 정보(모델/OS/IP/저장소 잔량), 로그 내보내기
6. **(후속) 백오피스 연동**
   - 서버 URL/기기 등록 토큰 입력 자리만 마련 — 실제 동작은 백오피스 단계에서 구현

### 4.2 동영상 플레이어

| 구분 | 내용 |
|---|---|
| 콘텐츠 소스 | 1차: 사전 탑재 로컬 파일. 폴더: 앱 전용 외부 저장소 `Android/data/com.dobedub.kiosk/files/videos/` (관리자가 USB/adb로 투입) |
| | 2차(후속): 백오피스에서 원격 다운로드/교체 |
| 재생 엔진 | **Media3 ExoPlayer** |
| 권장 포맷 | MP4 (H.264 + AAC), 1080p 이하 — Snapdragon 662 하드웨어 디코딩 범위 |
| 메타데이터 | 파일명 = 표시 제목(정렬 순서 접두 번호 허용: `01_제목.mp4`). 썸네일은 첫 프레임 자동 추출, 동일 파일명 `.jpg` 있으면 우선 사용 |

**UI 요구사항**

- 목록: 썸네일 카드 그리드(가로 4열 내외). 카드에 썸네일 + 제목 + 재생시간. 피그마의 카드/칩 스타일 준용.
- 재생: 탭 즉시 전체화면 재생. 컨트롤은 대형 버튼 3개 수준으로 최소화 — **재생/일시정지, 처음부터, 목록으로**. 시크바 제공(어린이 오조작 고려해 컨트롤 자동 숨김 3초).
- 재생 종료 시: 다음 영상 자동 재생 여부는 관리자 설정(기본 OFF, 종료 후 목록 복귀).
- 무조작 N분 시 목록 → 홈 복귀.

### 4.3 웹사이트 접근 (제한 브라우저)

| 구분 | 내용 |
|---|---|
| 엔진 | Android WebView (임베디드) — 일반 브라우저 앱 사용 금지 |
| 시작 페이지 | 관리자 설정의 시작 URL (예: 보이스툰 도서관 서비스) |
| 접근 정책 | **도메인 화이트리스트** — 허용 도메인(서브도메인 포함) 외 모든 내비게이션 차단 |

**차단 항목 (필수)**

- 주소창/검색창 없음 — URL 입력 수단 자체를 제공하지 않음
- 방문 기록/북마크/탭 기능 없음
- `shouldOverrideUrlLoading`에서 화이트리스트 외 URL 차단(무반응 또는 안내 토스트)
- 다운로드 차단(`DownloadListener`에서 무시)
- 외부 앱 인텐트(`intent://`, `market://`, `tel:` 등) 차단
- `window.open` 새 창 → 같은 웹뷰 내 로드로 강제(화이트리스트 검사 동일 적용)
- 롱프레스 컨텍스트 메뉴(이미지 저장/링크 복사) 차단
- 파일 선택기(`<input type="file">`) 차단
- 쿠키/스토리지: 세션 단위 운용 — 무조작 복귀 시 쿠키·히스토리·웹스토리지 삭제(공용 기기 개인정보 보호)

**제공 항목 (최소 내비게이션)**

- 상단 슬림 바: 홈(시작 URL로), 뒤로가기, 새로고침 — 3버튼만. 피그마 헤더/칩 스타일 준용
- 로딩 인디케이터(Primary 색), 오류 페이지(네트워크 오류 시 재시도 버튼)

---

## 5. 디자인 가이드 (피그마 기준)

- 기준 파일: **두비덥 도서관** (fileKey `WicfHebMZ40Kp4lgmmMqzQ`, 페이지 `00. Home`, node `29-7962`)
- 원칙: **피그마에 존재하는 토큰·컴포넌트를 우선 사용**하고, 없는 요소는 아래 토큰 체계에 맞춰 임의 생성한다.
- 디자인 시스템 구조는 원티드 Montage 체계를 따름(칩/아이콘버튼/푸시배지 등 컴포넌트 문서: montage.wanted.co.kr).

### 5.1 색상 토큰 (피그마 추출값)

| 토큰 | 값 | 용도 |
|---|---|---|
| `Primary/Normal` | `#ACF537` | 브랜드 라임 그린 — 강조 배경, 활성 요소 |
| `Primary/Heavy` | `#8AC42C` | 진한 그린 — 활성 텍스트, 채워진 칩/버튼 |
| `Background/Normal` | `#FFFFFF` | 기본 배경. 헤더는 88% 불투명 + 배경 블러 |
| `Label/Normal` | `#171719` (`#0F0F10`) | 본문/제목 텍스트 |
| `Line/Neutral` | `rgba(112,115,124,0.16)` | 칩/카드 테두리, 구분선 |
| `Accent/Red` | `#CC2222` | 푸시 배지, 오류 표시 |
| 히어로/서브 배경 | 연한 라임 톤(약 `#EAF6D2` 계열) | 배너·타일 배경 — 피그마 배너 스크린샷 기준 임의 보정 허용 |

투명도 토큰: `opacity/88`(헤더 배경), `opacity/43`(칩 테두리), `opacity/5`(칩 배경 틴트).

### 5.2 타이포그래피

| 스타일 | 값 |
|---|---|
| 서체 | **Pretendard** (전 화면 공통) |
| Body2 / Medium | 15px, line-height 1.467, letter-spacing +0.96% — 칩·본문 |
| Label1 / Medium | 14px, line-height 1.429, letter-spacing +1.45% — 보조 라벨 |
| 키오스크 확장 | 태블릿 대형 타일 제목·재생 버튼 등은 동일 서체로 24~32px 스케일업(임의 생성 범위) |

### 5.3 컴포넌트 스타일

| 컴포넌트 | 스펙 |
|---|---|
| 헤더 | 높이 56px, 흰 배경 88% + backdrop blur, 좌측 로고(100×32), 우측 아이콘 버튼(24px)·라운드 칩 |
| RoundChip | radius 16px, 테두리 Primary 43%, 배경 Primary 5% 틴트, 텍스트 `Primary/Heavy` |
| SquareChip (탭) | radius 10px, 기본: `Line/Neutral` 테두리 + `Label/Normal` 텍스트 / **활성: 그린 채움 + 흰 텍스트** |
| IconButton | 24px 아이콘, 인터랙션 시 원형(1000px) 오버레이, 우상단 푸시 배지(빨강) 옵션 |
| 인터랙션 오버레이 | pressed 시 불투명 오버레이(Light: Normal×0.75 가중치) — 전 컴포넌트 공통 규칙 |

### 5.4 아이콘

- 피그마 아이콘 세트(`Icons/Icons` 컴포넌트) 사용: Search, Noti(알림), Book(서재), Movie(동영상), Magic Mic(마이 보이스) 등 확인됨.
- **개발 시 피그마에서 SVG로 export하여 Vector Drawable로 변환** 사용.
- 키오스크에 필요하지만 세트에 없는 아이콘(설정 톱니, 잠금, 재생/일시정지, 새로고침 등)은 동일한 스트로크 굵기·라운드 스타일로 임의 제작.

### 5.5 태블릿 레이아웃 대응

- 피그마 원본은 모바일(393pt) 기준 → 본 프로젝트는 **가로 2000×1200 고정(landscape)**.
- 헤더·칩 등 컴포넌트 스펙은 그대로 쓰되, 콘텐츠 영역은 최대 폭 컨테이너(피그마 `max-w 1023px` 규칙 참고) + 좌우 여백 확장으로 재배치.
- 홈 타일·재생 컨트롤은 터치 타깃 최소 64dp 이상(공용 키오스크 기준, 임의 생성 범위).

---

## 6. 기술 스택

| 영역 | 선택 | 사유 |
|---|---|---|
| 플랫폼 | Android 네이티브 (**Kotlin**), 단일 APK | Device Owner/LockTask 등 잠금 API는 네이티브 필수. 크로스플랫폼 불필요(단일 기기) |
| SDK 범위 | minSdk 29 (Android 10) / targetSdk 최신 | TB-J606F 출고 OS가 10 |
| UI | Jetpack Compose | 화면 수 적고 커스텀 스타일 많음 — 토큰 기반 테마 구성에 유리 |
| 영상 | Media3 (ExoPlayer) | 안정적 로컬 재생, 썸네일 추출 |
| 웹 | AndroidX WebView + WebViewClient/WebChromeClient 커스텀 | 화이트리스트·차단 정책 세밀 제어 |
| 설정 저장 | Jetpack DataStore (Preferences) | PIN 해시, URL 목록, 타이머 등 |
| 잠금 | DevicePolicyManager (Device Owner) | §2 참조 |
| 배포 | 사이드로드 APK + adb 프로비저닝 스크립트 | 스토어 배포 불필요, 납품 절차에 스크립트 포함 |

### 6.1 설정 데이터 스키마 (초안)

```json
{
  "adminPinHash": "…",
  "adminPinSalt": "…",
  "startUrl": "https://…",
  "allowedDomains": ["dobedub.com", "vogopang.com"],
  "videoDirOverride": null,
  "idleTimeoutMinutes": 5,
  "autoPlayNext": false,
  "volumeMax": 70,
  "brightness": 80,
  "operatingHours": { "enabled": false, "start": "09:00", "end": "18:00" },
  "backofficeUrl": null,
  "deviceToken": null
}
```

---

## 7. 비기능 요구사항

- **복구성**: 앱 크래시·재부팅·전원 껐다 켬 어느 경우에도 사용자 개입 없이 키오스크 홈으로 복귀해야 한다.
- **무인 운영**: 24시간 전원 연결 상시 운용 가정. 운영 시간 외 절전(화면 어둡게)으로 발열·수명 관리.
- **개인정보**: 웹뷰 세션 데이터는 무조작 복귀 시 삭제. 앱 자체는 이용자 개인정보를 수집하지 않는다.
- **오프라인 내성**: 네트워크 단절 시 동영상 기능은 정상 동작, 웹 기능은 오류 화면 + 재시도 안내.
- **접근성**: 어린이 이용 고려 — 대형 터치 타깃, 낮은 텍스트 의존, 명확한 아이콘.

---

## 8. 개발 로드맵

| Phase | 내용 | 완료 기준 |
|---|---|---|
| **1. 키오스크 셸** | 커스텀 런처 + Device Owner + LockTask, 홈 화면 골격, 관리자 PIN 진입/해제, 프로비저닝 스크립트 | 재부팅·크래시에도 키오스크 유지, 관리자만 해제 가능 |
| **2. 동영상 플레이어** | 로컬 폴더 스캔, 썸네일 그리드, ExoPlayer 전체화면 재생, 무조작 복귀 | 탑재 영상 목록·재생·복귀 정상 동작 |
| **3. 제한 웹뷰** | 화이트리스트 내비게이션, 차단 정책 전체, 최소 내비게이션 바, 세션 초기화 | 화이트리스트 외 이동 불가 검증 시나리오 통과 |
| **4. 디자인/폴리싱** | 피그마 토큰 테마 적용, 관리자 설정 전체 화면, 운영시간 절전, QA | 디자인 가이드 §5 반영, 납품 체크리스트 통과 |
| **5. (후속) 백오피스 연동** | 기기 등록, 원격 동영상 배포, 설정 원격 변경, 상태 모니터링 | 별도 기획 |

---

## 9. 리스크 및 열린 이슈

| # | 이슈 | 대응 |
|---|---|---|
| 1 | **전원 버튼은 물리적으로 차단 불가** — 길게 눌러 강제 종료 가능 | 재부팅 시 자동 키오스크 복귀로 실효성 제거. 필요 시 전원 메뉴 숨김(LockTask 기능 플래그) + 스탠드 구조로 버튼 접근 억제 |
| 2 | Device Owner는 **공장초기화 직후에만 설정 가능** | 납품 준비 절차에 명시. 이미 사용 중 기기는 초기화 필요 |
| 3 | TB-J606F OS 버전 편차(10/11/12) | minSdk 29 기준 개발 + 납품 전 동일 버전으로 통일 업데이트 권장 |
| 4 | 대상 웹사이트가 외부 도메인 리소스(CDN, 결제, OAuth 등)를 쓰는 경우 | 시작 URL 확정(`https://splib.dobedub.com/home`), 기본 허용 도메인 `splib.dobedub.com`(서브도메인 자동 허용) 적용됨. **외부 CDN/결제/OAuth 등 추가 도메인 실사는 여전히 필요 (열린 이슈)** — 실제 사이트 접속 시 화면이 깨지거나 로그인이 막히면 관리자 화면에서 허용 도메인 추가 |
| 5 | 동영상 원격 배포 전까지 콘텐츠 교체는 수동(USB/adb) | Phase 5에서 해소. 그 전까지 관리자 매뉴얼에 교체 절차 문서화 |
| 6 | Wi-Fi 전용 모델 — 도서관 망 정책(캡티브 포털, 방화벽) | 설치 시 현장 네트워크 점검 항목에 포함 |
| 7 | 관리자 PIN 분실 | adb(USB 디버깅은 차단 상태) 불가 → **복구 절차 정의 필요 (열린 이슈)**: 예) 두비덥 발급 마스터 코드(기기 시리얼 기반) |
| 8 | ~~웹뷰가 모바일 레이아웃으로 나와 태블릿 화면을 못 채우고, 웹툰 리더 오디오-스크롤 싱크가 어긋남~~ | **해결(2026-07-12)**: 데스크탑 UA 적용 + 웹툰 리더 컬럼 폭 강제 확장 + 스크롤 좌표 보정 어댑터 추가. 실기기 확인 완료. 상세는 `DobedubKiosk/README.md` "제한 웹뷰 — 웹툰 리더 데스크탑 렌더링/싱크 보정" 참고. 사이트 프론트엔드 리뉴얼 시 DOM 구조 의존성 재확인 필요 |

### 확인 필요 사항 (남은 것)

1. ~~웹사이트 시작 URL~~ → 확정됨: `https://splib.dobedub.com/home`
2. ~~샘플 동영상~~ → 확정됨: `[두비덥 보이스툰] 그리스 로마 신화 - 아르테미스의 분노와 사랑 1` (mp4). 실 납품용 전체 수량·해상도·총용량은 추후 확인 필요
3. 도서관 측 운영 시간 절전 기능의 1차 포함 여부
4. 관리자 PIN 분실 시 복구 정책 — 현재 구현은 기본 PIN(`0000`)에서 미변경 시 그 값으로 계속 진입 가능. 변경 후 분실 시 복구 수단 없음(공장초기화 후 재설정만 가능) — **정책 확정 필요**
5. 외부 CDN/결제/OAuth 등 splib.dobedub.com 외 추가 허용 도메인 실사

---

## 10. 납품 프로비저닝 절차

**자동화 스크립트로 구현 완료**: `DobedubKiosk/provision-kiosk.ps1` — 아래 1~2단계 후 실행하면 3~4단계(설치, Device Owner 지정, 샘플 동영상 투입)를 자동 수행한다.

1. 기기 공장초기화 → 초기 설정에서 계정 추가 없이 Wi-Fi만 연결 (또는 건너뛰기)
2. USB로 PC와 연결, USB 디버깅 허용
3. `./provision-kiosk.ps1` 실행 → APK 설치 + `adb shell dpm set-device-owner com.dobedub.kiosk/.kiosk.AdminReceiver` + 샘플 동영상 push
4. 앱 최초 실행 → 로고 5회 탭 → 기본 PIN(`0000`)으로 관리자 진입 → PIN 변경, 시작 URL/허용 도메인 확인
5. 최초 실행 시 자동으로 키오스크(Lock Task) 진입됨 → 재부팅 테스트(자동 복귀 확인) → 잠금 우회 시도 체크리스트 검증
6. 현장 설치: 전원 상시 연결, 스탠드 고정, 도서관 Wi-Fi 연결 확인
