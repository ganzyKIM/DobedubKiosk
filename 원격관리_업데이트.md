# 원격 관리 · 앱 자동 업데이트

배포된 태블릿들을 **현장 방문 없이 일괄 업데이트**하고, **접속·버전 현황을 백오피스에서
조회하고, 영상을 원격 배포**하는 시스템.

```
                    ┌──────────────── 함대 관리 서버 (server/) ────────────────┐
  태블릿(앱)         │  POST /api/checkin  →  SQLite(기기 인벤토리)             │
   ├─ 30분마다 체크인 ─┤  응답: 매니페스트 + 영상 삭제/배포 지시 + 강제알림 여부   │
   ├─ 새 버전이면 ──────┤  GET /download/app.apk                                  │
   │   무인 설치        │  GET /media/:id/download (영상)                        │
   └─ (Device Owner)   │  /dashboard  ← 관리자(공유 비밀번호)                     │
                    └──────────────────────────────────────────────────────────┘
```

## 0. 대전제 — 서명 키 (가장 중요)

안드로이드는 **같은 키로 서명된 APK만** 기존 앱 위에 업데이트할 수 있다. 그래서:

- 이 프로젝트는 고정 **릴리스 키스토어**(`release-keystore.jks` + `keystore.properties`)로 서명한다.
- **이 두 파일을 절대 잃어버리면 안 된다.** 분실 시 이미 배포된 기기를 다시는 자동 업데이트할 수 없다
  (기기마다 공장초기화 + 재설치가 필요해짐). 안전한 곳(비밀번호 관리자/보안 드라이브)에 **백업**하고,
  빌드하는 각 PC의 프로젝트 루트에 복사해 둔다. (git 에는 올라가지 않음 — `.gitignore` 처리)
- 키스토어가 없는 PC에서 `assembleRelease` 하면 디버그 키로 폴백되어 빌드는 되지만, 그 APK는
  기존 기기와 서명이 달라 **업데이트에 쓸 수 없다.** 운영 빌드는 반드시 키스토어 보유 PC에서.

납품용 초기 설치(`태블릿-세팅.bat`)도 릴리스 APK가 있으면 자동으로 그것을 사용한다
(초기 설치와 이후 업데이트의 서명이 같아야 하므로).

### 이미 배포된(서명이 다른) 기기 옮기기 — 1회 마이그레이션
기존에 **다른 키(디버그/타 PC)로 설치된** 기기는 새 릴리스 키 APK와 서명이 달라 덮어쓰기가 안 된다
(`INSTALL_FAILED_UPDATE_INCOMPATIBLE`). 이런 기기는 **한 번만** 아래로 옮긴다(이후부터는 원격 자동 업데이트):
1. 태블릿에서 관리자 진입(로고 5탭 → PIN) → **키오스크 관리 > 키오스크 완전 해제**(Device Owner 반환)
2. `adb uninstall com.dobedub.kiosk` (또는 설정에서 앱 삭제)
3. `태블릿-세팅.bat` 재실행 → 릴리스 APK로 재설치·재프로비저닝
> 완전 해제가 불가한 경우(PIN 분실 등)는 공장초기화 후 재세팅. 어차피 물리 접근이 필요한 1회 작업이며,
> 이후 릴리스 키로 통일된 기기들은 현장 방문 없이 원격 업데이트된다.

## 1. 앱 쪽 동작 (자동 업데이터)

- 앱은 시작 20초 후 1회 + 이후 **30분마다** 서버에 체크인한다(`AppUpdater`).
  이 주기는 `MainActivity.UPDATE_CHECK_INTERVAL_MS` 와 서버의 `CHECKIN_INTERVAL_MS`가
  같은 값이어야 한다 — 대시보드의 온라인/오프라인 판정이 이 값에서 파생되기 때문이다.
- 체크인 payload: 기기ID(ANDROID_ID)·모델·시리얼·앱 버전·배터리·키오스크 잠금 여부·시작 URL·
  기관 라벨·보유 영상 목록(파일명+크기).
- 응답의 `versionCode` 가 현재보다 크면 두 갈래로 나뉜다:
  - **평소(관리자가 알림을 요청하지 않음)**: 홈 화면 유휴 상태일 때 APK 다운로드 →
    **sha256 검증** → **조용히 무인 설치**(사용자 확인 없음). 사용 중이면 다음 주기로 미룸.
  - **관리자가 "업데이트 알림 보내기"를 요청한 기기**: 홈 화면에 확인창을 띄우고,
    사용자가 "지금 업데이트"를 눌러야 그때 설치를 시작한다(`AppUpdater.Result.NeedsConfirmation`).
    관리자 화면의 "지금 업데이트 확인" 버튼은 누르는 것 자체가 동의이므로 확인창 없이 즉시 설치한다.
- 무인 설치가 가능한 이유: 앱이 **Device Owner** 라 `PackageInstaller` 커밋에 사용자 확인창이 없다.
  설치되면 프로세스가 재시작되고, 영속 HOME 설정으로 키오스크가 곧바로 복귀한다.
- 영상도 같은 체크인 응답으로 삭제/배포 지시를 받아 백그라운드에서 처리한다(§2 참조).

### 서버 주소 설정
- 빌드 기본값: `app/build.gradle.kts` 의 `fleetServerUrl` (기본 `https://kiosk.dobedub.com`).
  빌드 시 `gradlew assembleRelease -PfleetServerUrl=https://...` 로 재정의 가능.
- 기기별로 관리자 화면 **"원격 관리 / 업데이트"** 에서 서버 주소·기관명을 덮어쓸 수 있다.
- **반드시 HTTPS 이거나 `dobedub.com`/`localhost` 도메인이어야 한다.** 앱의
  `network_security_config.xml`이 그 외 평문 HTTP(사설 LAN IP 등)를 아예 차단한다.
  같은 사무실 Wi-Fi라도 `http://192.168.x.x:8090` 같은 주소는 접속이 막힌다 — 반드시
  HTTPS 주소를 써야 하며, 개발 중엔 §2-1의 Cloudflare Tunnel로 만든 주소를 쓴다.

## 2. 서버 / 백오피스

`server/` 폴더의 Node.js + SQLite 서버. 실행·배포·API는 [server/README.md](server/README.md) 참고.

대시보드(`/dashboard`, 공유 비밀번호 로그인)에서:
- **KPI**: 전체 기기 수, 온라인, 오프라인(24시간+), 최신 버전 비율
  (온라인 임계값은 체크인 주기의 2배+여유 — 서버 코드에서 자동 계산)
- **배포 이력**: 업로드마다 이력에 남고, 예전 버전 행에서 **"이 버전으로 롤백"**을 누르면
  재업로드 없이 그 버전으로 되돌아간다(디스크에 파일이 남아 있을 때만 가능).
- **업데이트 알림 보내기**: "업데이트 필요 기기(N대)에 알림 보내기"(전체) 또는 기기별
  "알림 보내기" 버튼. 요청받은 기기만 홈 화면에 확인창이 뜬다(§1 참조).
- **버전 분포**: 어떤 버전이 몇 대인지 막대 그래프
- **기기 목록**: 기기ID·기관명(직접 입력)·모델·버전·잠금/배터리·**영상 목록**·업데이트 알림
  상태·마지막 접속시각. 문제 있는 기기(오프라인/구버전)가 목록 위로 정렬된다.
- **영상 자료실**: 영상을 한 번 업로드해두고, 기기 목록에서 원하는 기기를 골라
  "이 기기로 보내기" → 다음 체크인 때 기기가 다운로드한다. 대용량(수 GB) 대비 체크인마다
  재확인하는 큐 방식이라 중간에 끊겨도 다음 체크인에서 이어 확인된다. 실제로 받으면
  (다음 체크인의 인벤토리에 나타나면) 대기열에서 자동으로 지워진다.
- **영상 원격 삭제**: 기기가 이미 가진 영상 옆 **[삭제]** 버튼 → 다음 체크인 때 기기가
  실제로 지운다. 삭제가 완료되면(다음 체크인에서 목록에 없으면) 지시가 자동 확정된다.

### 2-1. 원격 접속 경로 (공인 HTTPS 주소)

**같은 와이파이**라면 주소를 칠 필요가 없다 — 관리자 화면의 "서버 자동 찾기" 가 서브넷을
스캔해(`/health` 가 `ok` 인 호스트) 자동 입력한다. 직접 칠 때도 `5` 처럼 마지막 자리만 넣으면
`http://<서브넷>.5:8090` 으로 펼쳐진다.

도서관에 나가 있는 태블릿은 **다른 네트워크**라 위 방법이 안 통한다. 이때만 **공인 HTTPS
주소**가 필요하다.

**지금 당장(테스트/임시)**: `server/start-tunnel.ps1` 실행 → Cloudflare Quick Tunnel이
`https://<임의문자열>.trycloudflare.com` 주소를 만들어준다. 이 주소를 기기 관리자 화면의
서버 주소에 입력하면 같은 Wi-Fi든 완전히 다른 네트워크든 상관없이 체크인된다(실기기로
검증됨: 재부팅 → 20초 후 자동 체크인 → 확인창 표시까지 확인).

> Quick Tunnel 주소는 **cloudflared를 재시작할 때마다 바뀐다.** 태블릿이 이미 도서관에
> 나가 있는 상태에서 주소가 바뀌면 원격으로 고칠 방법이 없다(다시 현장 방문 필요) —
> 그래서 이 방식은 개발/테스트 전용이다.

**실제 배포 전 반드시 고정 주소로 전환**: 아래 중 하나.
(검토했다가 탈락한 것 — ngrok 무료는 월 1GB 제한이라 영상 배포 불가,
 localtunnel 은 태블릿에서 HTTP 511 인증 안내 페이지에 막힘.)
- Cloudflare **named tunnel**로 고정 서브도메인 발급(무료, 도메인 소유 필요).
- 사내 `kiosk.dobedub.com`을 이 서버(또는 나중에 옮길 실서버)로 향하게 함 — 이게 가장
  자연스럽다. 앱의 기본값이 이미 `https://kiosk.dobedub.com`이라 **서버 주소 칸을
  비워두기만 하면** 자동으로 이 주소를 쓴다. DNS만 바꾸면 되므로 서버를 나중에
  사무실 PC → AWS로 옮겨도 태블릿은 손댈 필요가 없다. Route 53 레코드 추가는 인프라
  담당 협조가 필요하다(`server/README.md` "보고팡 인프라로 이전할 때" 참조).

## 3. 새 버전 내보내기 (요약)

```
1) app/build.gradle.kts 의 versionCode +1, versionName 갱신
2) gradlew assembleRelease            (키스토어 보유 PC)
3) 대시보드 → 새 APK 업로드 (같은 versionCode/versionName 입력) → 이력에 추가되며 즉시 배포
4) 기기들이 다음 체크인 때 자동 업데이트 (재부팅하면 즉시)
5) (선택) 빨리 반영하고 싶으면 "업데이트 필요 기기에 알림 보내기"로 확인창을 강제로 띄운다
```

문제가 생기면 대시보드 "배포 이력"에서 이전 버전 행의 **"이 버전으로 롤백"**을 누른다
(파일이 지워지지 않았다면 재업로드 없이 바로 되돌아간다).

## 4. 한계 / 향후

- 소규모 함대(수십 대) 기준의 단순·자체 호스팅 구성이다.
- 원격 설정 푸시(시작 URL·화이트리스트 원격 변경)·원격 초기화·상세 로그까지 필요해지면
  정식 EMM(Android Management API 등) 도입을 검토한다.
- 지금은 Cloudflare Quick Tunnel(임시 주소)로 원격 접속을 검증한 상태 — 실제 배포 전
  §2-1의 고정 주소 전환이 남아 있다.
