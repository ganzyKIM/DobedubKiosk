# 원격 관리 · 앱 자동 업데이트

배포된 태블릿들을 **현장 방문 없이 일괄 업데이트**하고, **접속·버전 현황을 백오피스에서 조회**하는 시스템.

```
                    ┌──────────────── 함대 관리 서버 (server/) ────────────────┐
  태블릿(앱)         │  POST /api/checkin  →  SQLite(기기 인벤토리)             │
   ├─ 6시간마다 체크인 ─┤  응답: 최신버전 매니페스트                              │
   ├─ 새 버전이면 ──────┤  GET /download/app.apk                                  │
   │   무인 설치        │                                                         │
   └─ (Device Owner)   │  /dashboard  ← 관리자(공유 비밀번호)                     │
                    └──────────────────────────────────────────────────────────┘
```

## 0. ⚠ 대전제 — 서명 키 (가장 중요)

안드로이드는 **같은 키로 서명된 APK만** 기존 앱 위에 업데이트할 수 있다. 그래서:

- 이 프로젝트는 고정 **릴리스 키스토어**(`release-keystore.jks` + `keystore.properties`)로 서명한다.
- **이 두 파일을 절대 잃어버리면 안 된다.** 분실 시 이미 배포된 기기를 다시는 자동 업데이트할 수 없다
  (기기마다 공장초기화 + 재설치가 필요해짐). 안전한 곳(비밀번호 관리자/보안 드라이브)에 **백업**하고,
  빌드하는 각 PC의 프로젝트 루트에 복사해 둔다. (git 에는 올라가지 않음 — `.gitignore` 처리)
- 키스토어가 없는 PC에서 `assembleRelease` 하면 디버그 키로 폴백되어 빌드는 되지만, 그 APK는
  기존 기기와 서명이 달라 **업데이트에 쓸 수 없다.** 운영 빌드는 반드시 키스토어 보유 PC에서.

납품용 초기 설치(`태블릿-세팅.bat`)도 릴리스 APK가 있으면 자동으로 그것을 사용한다
(초기 설치와 이후 업데이트의 서명이 같아야 하므로).

### 이미 배포된(서명이 다른) 기기 옮기기 — 1회 마이그레이션
기존에 **다른 키(디버그/타 PC)로 설치된** 기기는 새 릴리스 키 APK와 서명이 달라 덮어쓰기가 안 된다
(`INSTALL_FAILED_UPDATE_INCOMPATIBLE`). 이런 기기는 **한 번만** 아래로 옮긴다(이후부터는 원격 자동 업데이트):
1. 태블릿에서 관리자 진입(로고 5탭 → PIN) → **키오스크 관리 > 키오스크 완전 해제**(Device Owner 반환)
2. `adb uninstall com.dobedub.kiosk` (또는 설정에서 앱 삭제)
3. `태블릿-세팅.bat` 재실행 → 릴리스 APK로 재설치·재프로비저닝
> 완전 해제가 불가한 경우(PIN 분실 등)는 공장초기화 후 재세팅. 어차피 물리 접근이 필요한 1회 작업이며,
> 이후 릴리스 키로 통일된 기기들은 현장 방문 없이 원격 업데이트된다.

## 1. 앱 쪽 동작 (자동 업데이터)

- 앱은 시작 20초 후 1회 + 이후 **6시간마다** 서버에 체크인한다(`AppUpdater`).
- 체크인 payload: 기기ID(ANDROID_ID)·모델·시리얼·앱 버전·배터리·키오스크 잠금 여부·시작 URL·기관 라벨.
- 응답의 `versionCode` 가 현재보다 크면: APK 다운로드 → **sha256 검증** → **Device Owner 무인 설치**.
- 재생 중 갑작스러운 재시작을 막기 위해, 자동 설치는 **홈 화면 유휴 상태일 때만** 진행한다
  (사용 중이면 다음 주기로 미룸). 관리자 화면의 "지금 업데이트 확인" 버튼은 즉시 설치한다.
- 무인 설치가 가능한 이유: 앱이 **Device Owner** 라 `PackageInstaller` 커밋에 사용자 확인창이 없다.
  설치되면 프로세스가 재시작되고, 영속 HOME 설정으로 키오스크가 곧바로 복귀한다.

### 서버 주소 설정
- 빌드 기본값: `app/build.gradle.kts` 의 `fleetServerUrl` (기본 `https://kiosk.dobedub.com`).
  빌드 시 `gradlew assembleRelease -PfleetServerUrl=https://...` 로 재정의 가능.
- 기기별로 관리자 화면 **"원격 관리 / 업데이트"** 에서 서버 주소·기관명을 덮어쓸 수 있다.

## 2. 서버 / 백오피스

`server/` 폴더의 Node.js + SQLite 서버. 실행·배포·API는 [server/README.md](server/README.md) 참고.

대시보드(`/dashboard`, 공유 비밀번호 로그인)에서:
- **KPI**: 전체 기기 수, 온라인(15분 내), 오프라인(24시간+), 최신 버전 비율
- **버전 분포**: 어떤 버전이 몇 대인지 막대 그래프
- **기기 목록**: 기기ID·기관명(직접 입력)·모델·버전·잠금/배터리·마지막 접속시각
- **APK 업로드**: 새 버전 APK + versionCode/versionName 입력 → 즉시 배포

## 3. 새 버전 내보내기 (요약)

```
1) app/build.gradle.kts 의 versionCode +1, versionName 갱신
2) gradlew assembleRelease            (키스토어 보유 PC)
3) 대시보드 → 새 APK 업로드 (같은 versionCode/versionName 입력)
4) 기기들이 다음 체크인 때 자동 업데이트 (재부팅하면 즉시)
```

## 4. 한계 / 향후

- 소규모 함대(수십 대) 기준의 단순·자체 호스팅 구성이다.
- 원격 설정 푸시(시작 URL·화이트리스트 원격 변경)·원격 초기화·상세 로그까지 필요해지면
  정식 EMM(Android Management API 등) 도입을 검토한다. 현재는 앱 업데이트 + 현황 조회에 집중.
