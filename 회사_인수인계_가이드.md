# 두비덥 보이스툰 키오스크 — 회사 인수인계 가이드 (윈도우 PC용)

**컴퓨터 전문 지식이 없어도 따라 할 수 있게** 썼습니다. 넷버드(NetBird)라는 낯선 프로그램이
나오는데, 그게 무엇이고 왜 필요한지부터 설명합니다.

> ### 읽는 순서
>
> | 부 | 내용 | 시간 |
> |---|---|---|
> | **0부** | 이게 무슨 시스템인가 (읽기만) | 3분 |
> | **1부** | 준비물 내려받기 (5개) | 20분 |
> | **2부** | 넷버드 계정·설정 | 20분 |
> | **3부** | 관리자 PC 준비 | 15분 |
> | **4부** | 태블릿 세팅 (대당) | 15분 |
> | **5부** | 평소 운영 | 매일 |
> | **6부** | 문제가 생겼을 때 | 필요할 때 |
>
> **0~4부는 순서대로 해야 합니다.** 건너뛰면 뒤에서 막힙니다.

---

# 0부. 이게 무슨 시스템인가

## 태블릿은 무엇을 하나

태블릿을 **딱 3가지만 되는 키오스크**로 잠급니다.

| 태블릿 화면 버튼 | 내용 |
|---|---|
| 🎬 **동영상** | 태블릿에 넣어둔 영상 재생 |
| 📖 **도서관** | 보이스툰 웹툰 보기 |
| 🎤 **마이보이스** | 아이가 직접 목소리를 입히는 더빙 |

아이는 이 3개 밖으로 못 나갑니다 — 홈 버튼, 다른 앱, 인터넷 주소창이 전부 막혀 있습니다.

## 관리자 PC는 무엇을 하나

**회사 PC 한 대**가 전국 도서관에 나가 있는 태블릿들을 **원격으로** 관리합니다.
영상 넣기, 앱 업데이트, 상태 확인, 재부팅까지 **현장에 가지 않고** 합니다.

```
   [회사 관리자 PC]                          [도서관 태블릿들]
    · 대시보드에서 상태 확인      ←─ 인터넷 ─→   · 10분마다 스스로 접속해서
    · 영상·업데이트 보내기                         지시를 받아감
```

## 그런데 왜 "넷버드"가 필요한가 ★중요

도서관 태블릿은 **그 도서관의 와이파이**를 씁니다. 회사 PC와는 완전히 다른 인터넷이죠.
보통은 이런 상태에서 서로 연결할 수 없습니다. 방법은 두 가지인데:

| 방법 | 문제 |
|---|---|
| 도서관마다 공유기 설정을 바꿔달라고 부탁 | 도서관이 안 해줍니다. 도서관마다 다 해야 합니다 |
| **넷버드 사용** ← 우리가 쓰는 방법 | 설정 부탁 없이, 프로그램만 깔면 됩니다 |

**넷버드는 회사 PC와 태블릿들을 "보이지 않는 하나의 사내망"으로 묶어주는 프로그램**입니다.
넷버드를 깔면 회사 PC와 태블릿에 각각 `100.` 으로 시작하는 **전용 주소**가 하나씩 생깁니다.
태블릿은 어느 도서관에 가 있든 **이 전용 주소로** 회사 PC를 찾아옵니다.

```
   회사 PC     100.109.70.91     ←── 넷버드 전용망 ──→   태블릿 A  100.109.230.149
   (진짜 인터넷 주소는 계속 바뀌어도, 이 100. 주소는 안 바뀝니다)
```

> **비유하자면**: 도서관마다 다른 우편함(공용 인터넷)을 쓰지만, 우리끼리는 사내 우편번호
> (100. 주소)를 하나씩 배정받아 그걸로 주고받는 것입니다.
>
> 안전성: 오가는 내용은 전부 암호화되고(WireGuard 방식), 태블릿은 **회사 PC의 관리
> 포트 하나**로만 접속할 수 있게 제한돼 있습니다.

## 3가지 역할을 구분하세요

| 이름 | 무엇 | 누가 씁니까 |
|---|---|---|
| **넷버드 콘솔** (웹) | 어느 기기가 전용망에 등록됐는지 보는 곳 | 가끔, 확인할 때만 |
| **관리 대시보드** (웹) | **매일 쓰는 화면.** 태블릿 상태·영상·업데이트 | 자주 |
| **태블릿 관리자 화면** | 태블릿에서 직접 여는 숨은 설정 | 현장에서 가끔 |

## ⚠ 먼저 알아야 할 것 — 이미 나가 있는 태블릿들 (군산 등)

**이 시스템을 쓰기 전에 이미 도서관에 설치된 태블릿들이 있습니다(군산 등).**
그 태블릿들은 **예전 관리자 PC**를 바라보도록 설정돼 있는데, 관리자가 이 회사 PC로
넘어오면서 **그 연결은 끊어집니다.**

### 무슨 일이 벌어지나

| | 새로 세팅한 태블릿 | **이미 나가 있는 태블릿** |
|---|---|---|
| 대시보드에 보이나 | ✅ 보입니다 | ❌ **안 보입니다** |
| 원격으로 영상·업데이트 | ✅ 됩니다 | ❌ 안 됩니다 |
| 태블릿 자체 동작 | 정상 | **정상** (아이들은 그대로 씁니다) |

> **중요**: 그 태블릿들이 **고장 난 게 아닙니다.** 아이들은 평소처럼 계속 사용할 수 있습니다.
> 다만 **원격 관리만** 안 되는 상태입니다. 대시보드에 안 보인다고 놀라지 마세요.

### 어떻게 해결하나 — 회수해서 다시 세팅하는 수밖에 없습니다

원격으로는 고칠 수 없습니다. 원격 관리가 끊긴 상태라 원격 지시가 닿지 않기 때문입니다.

```
   ① 그 태블릿을 회사로 회수
   ② 이 가이드의 4부(태블릿 세팅)를 그대로 다시 실행
   ③ 대시보드에 나타나는지 확인
   ④ 도서관에 다시 설치
```

4부를 다시 돌리면 **넷버드 등록 + 새 관리 서버 주소**가 심어져서 이후로는 원격 관리가 됩니다.
한 번만 하면 되고, 그 뒤로는 회수할 일이 없습니다.

> 💡 회수 계획을 세울 때: 도서관 방문 일정이나 정기 점검 때 함께 처리하면 됩니다.
> 급하지 않습니다 — 그때까지 태블릿은 정상 동작합니다.

---

# 1부. 준비물 내려받기 (5개)

**받은 파일은 전부 `DobedubKiosk-Operations` 폴더 안에 넣으세요.** 세팅 프로그램이 그 폴더에서
찾습니다. (`netbird-setup-key.txt` 만 2부에서 직접 만듭니다.)

| # | 준비물 | 왜 필요한가 | 어디서 |
|---|---|---|---|
| 1 | **Node.js** | 관리 서버를 돌리는 엔진 | https://nodejs.org |
| 2 | **platform-tools** | PC가 태블릿과 USB로 대화하는 도구 | https://developer.android.com/tools/releases/platform-tools |
| 3 | **넷버드 (윈도우용)** | 회사 PC를 전용망에 연결 | https://app.netbird.io (2부에서) |
| 4 | **넷버드 (안드로이드 APK)** | 태블릿을 전용망에 연결 | https://github.com/netbirdio/android-client/releases |
| 5 | **ADBKeyboard.apk** | 태블릿에 키를 자동 입력할 때 필요 | **패키지에 이미 포함돼 있습니다** |

## 1-1. Node.js 설치

1. https://nodejs.org 접속
2. 큰 버튼 중 **`LTS`** 라고 적힌 쪽을 클릭 → 파일이 받아집니다
3. 받은 파일을 실행 → **[Next] 만 계속 누르면 됩니다** (설정 바꿀 것 없음)
4. 마지막에 [Install] → [Finish]

> 버전 숫자는 신경 쓰지 마세요. 사이트가 권하는 LTS 를 그대로 받으면 됩니다.

## 1-2. platform-tools 내려받기 (adb)

태블릿을 USB로 세팅하려면 **adb** 라는 도구가 필요합니다. 설치 프로그램이 아니라
**압축을 풀어서 폴더에 넣기만** 하면 됩니다.

1. https://developer.android.com/tools/releases/platform-tools 접속
2. 페이지에서 **"Download SDK Platform-Tools for Windows"** 를 클릭
3. 약관 동의 체크 → 다운로드 (약 15MB)
4. 받은 zip 을 풀면 **`platform-tools`** 폴더가 나옵니다
5. 그 **`platform-tools` 폴더를 통째로** `DobedubKiosk-Operations` 폴더 안에 넣으세요

```
DobedubKiosk-Operations\
├── platform-tools\      ← 이렇게 들어가면 됩니다
│   ├── adb.exe
│   └── (여러 파일)
├── 태블릿-세팅.bat
├── 관리자-실행.bat
└── ...
```

> 폴더 이름을 바꾸지 마세요. `platform-tools` 그대로여야 자동으로 찾습니다.

## 1-3. 넷버드 안드로이드 APK 내려받기

태블릿에 설치할 넷버드입니다. (플레이스토어를 쓸 수 없는 기기라 파일로 받습니다.)

1. https://github.com/netbirdio/android-client/releases 접속
2. 맨 위 항목의 **`Assets`** 를 클릭해 펼칩니다
3. **`.apk`** 로 끝나는 파일을 클릭해 받습니다 (약 200MB)
4. 받은 파일을 `DobedubKiosk-Operations` 폴더 안에 넣습니다
   - 파일 이름이 `netbird-` 로 시작해야 합니다. 아니라면 `netbird-v0.5.0.apk` 처럼 바꿔주세요

> 검증된 버전은 **v0.5.0** 입니다. 더 최신 버전이 있어도 우선 이 버전을 권장합니다
> (최신 버전은 동작 확인이 안 돼 있습니다).

## 1-4. 준비물 확인

폴더가 이렇게 되면 준비 완료입니다:

```
DobedubKiosk-Operations\
├── platform-tools\             ← 1-2에서 넣음
├── netbird-v0.5.0.apk          ← 1-3에서 넣음
├── ADBKeyboard.apk             ← 원래 있음
├── netbird-setup-key.txt       ← 2부에서 만듭니다 (아직 없어도 됨)
├── 서버-준비.bat
├── 관리자-실행.bat
├── 태블릿-세팅.bat
└── ...
```

---

# 2부. 넷버드 계정·설정

## 2-1. 계정 로그인

넷버드 계정은 회사 계정 **하나**를 함께 씁니다.

- 계정: **devops@dobedub.com**
- 비밀번호(또는 로그인 방법): **담당자에게 문의**

1. https://app.netbird.io 접속
2. 위 계정으로 로그인
3. 로그인되면 **Peers**(연결된 기기 목록) 화면이 나옵니다

> 처음이라면 담당자에게 **초대**를 요청하세요. 담당자가 콘솔에서
> Team → Users → Invite User 로 초대 메일을 보낼 수 있습니다.

## 2-2. 콘솔에서 알아야 할 화면 3개

주소를 직접 입력하면 확실합니다.

| 화면 | 주소 | 무엇 |
|---|---|---|
| **Peers** | https://app.netbird.io/peers | 전용망에 등록된 기기 목록 (PC·태블릿) |
| **Setup Keys** | https://app.netbird.io/setup-keys | 태블릿을 자동 등록할 때 쓰는 열쇠 |
| **Access Control** | https://app.netbird.io/access-control | 누가 누구에게 접속 가능한지 규칙 |

## 2-3. 태블릿 등록용 열쇠(Setup Key) 준비

태블릿마다 사람이 로그인할 수는 없으니, **열쇠 문자열 하나**로 자동 등록합니다.

1. https://app.netbird.io/setup-keys 접속
2. 이미 **`kiosk-fleet`** 이라는 키가 있으면 그것을 씁니다
   - 키 값은 **만들 때 한 번만** 보입니다. 안 적어뒀다면 새로 만드세요(아래)
3. 없거나 값을 모르면 → 오른쪽 위 **Create Setup Key**
   - **Name**: `kiosk-fleet`
   - **Usage**: **Reusable**(여러 대에 재사용) 선택
   - **Expires**: 1년 등 넉넉히
   - **Auto-assign groups**: **`kiosk`** 선택 ★반드시
   - 만들기 → **화면에 뜬 긴 문자열을 복사** (다시 못 봅니다)
4. 메모장을 열어 그 문자열만 붙여넣고, **`netbird-setup-key.txt`** 이름으로
   `DobedubKiosk-Operations` 폴더에 저장

> 🔐 **이 파일은 비밀번호와 같습니다.** 메일·카톡으로 돌리지 마세요.
> 유출되면 콘솔에서 그 키를 **Revoke(폐기)** 하고 새로 만들면 됩니다.
>
> ★ **Auto-assign groups 에 `kiosk` 를 꼭 넣으세요.** 이게 빠지면 태블릿이 등록은 되는데
> 회사 PC에 접속할 권한이 없어서 대시보드에 안 나타납니다.

## 2-4. 관리자 PC를 전용망에 연결

1. https://app.netbird.io/peers 접속 → 오른쪽 위 **Add Peer**
2. **Windows** 선택 → 안내에 나온 설치 파일 다운로드 → 설치
3. 설치 후 넷버드 앱이 열리면 **Connect**(연결) → 브라우저가 열리며 로그인 요청
4. 같은 계정(devops@dobedub.com)으로 로그인 → **Connected** 표시 확인
5. **Peers 화면에 이 PC가 나타나는지** 확인

### ★ 반드시 해야 하는 설정 2가지

이 두 가지를 빠뜨리면 **다음 날 원격 관리가 끊깁니다.**

**① 그룹을 `fleet-server` 로 지정**

1. https://app.netbird.io/peers → 방금 추가된 이 PC 이름 클릭
2. **Groups** 항목에 **`fleet-server`** 추가 → 저장

> 왜: 태블릿(`kiosk` 그룹)은 **`fleet-server` 그룹에게만** 접속하도록 규칙이 걸려 있습니다.
> 그룹이 안 맞으면 태블릿이 이 PC를 못 찾습니다.

**② 자동 로그아웃(Login Expiration) 끄기**

넷버드는 기본적으로 **하루가 지나면 로그인이 풀립니다.** 사람이 쓰는 노트북이면 괜찮지만,
24시간 돌아야 하는 관리 PC에는 치명적입니다.

1. https://app.netbird.io/peers → 이 PC 이름 클릭
2. **Login Expiration**(로그인 만료) 를 **끄기 / Disabled** 로 변경 → 저장

> 태블릿은 열쇠(Setup Key)로 등록돼서 이 설정이 필요 없습니다. **관리 PC만** 하면 됩니다.

## 2-5-1. ⚠ "열쇠가 만료되면 도서관 태블릿이 다 끊기나요?" — 아닙니다

가장 많이 걱정하시는 부분이라 먼저 정리합니다. **결론: 안 끊깁니다.**

### 왜 안 끊기나

열쇠(Setup Key)는 **등록할 때 딱 한 번 쓰는 초대장**입니다.
태블릿이 한 번 등록되고 나면 자기만의 신분증을 발급받아서, 그 뒤로는 열쇠와 무관하게
동작합니다. 아파트 입주할 때 쓴 계약서가 만료돼도 이미 사는 사람은 안 쫓겨나는 것과 같습니다.

| 열쇠가 만료되면 | 결과 |
|---|---|
| 이미 등록된 태블릿 (도서관에 나가 있는 것들) | **아무 영향 없음. 계속 정상 동작** |
| 새 태블릿을 등록하려 할 때 | 등록이 안 됩니다 → 콘솔에서 새 열쇠를 만들면 해결 |

> **실제 확인**: 개발용 태블릿을 2026년 8월 15일에 이 열쇠로 등록한 뒤,
> 8월 31일까지 **16일 동안 재인증 한 번 없이** 계속 연결돼 있었습니다.

### 열쇠가 만료됐을 때 할 일 (새 태블릿을 세팅할 때만)

1. https://app.netbird.io/setup-keys 접속
2. 새 열쇠 만들기 (2-3의 절차 그대로, **Auto-assign groups 에 `kiosk` 필수**)
3. `netbird-setup-key.txt` 파일 내용을 새 값으로 교체
4. 만료된 옛 열쇠는 **Revoke(폐기)** 로 정리

### 진짜로 주의해야 할 만료는 따로 있습니다 — 관리 PC 쪽

헷갈리기 쉬운데, **끊길 수 있는 건 태블릿이 아니라 관리 PC**입니다.
관리 PC는 사람 계정으로 로그인해서 붙기 때문에 **하루 지나면 로그인이 풀립니다.**
그래서 2-4의 ②(**Login Expiration 끄기**)를 반드시 해야 합니다.

| 대상 | 등록 방식 | 만료되나 |
|---|---|---|
| **관리 PC** | 사람 계정 로그인(SSO) | ⚠ **하루 뒤 풀림** → 2-4의 ②로 반드시 꺼야 함 |
| **태블릿** | 열쇠(Setup Key) | 안 풀림 (16일 실측 확인) |

> 💡 그래도 확실히 해두고 싶으시면: 콘솔 Peers 에서 **각 태블릿을 눌러 Login Expiration 이
> 꺼져 있는지** 확인해 두세요. 10초면 되고, "어느 날 갑자기 전부 끊기는" 불안을 없앨 수 있습니다.

## 2-5. 이 PC의 전용 주소 확인 — 꼭 메모하세요

넷버드 앱 화면이나 Peers 목록에서 이 PC의 **`100.` 으로 시작하는 주소**를 확인해 적어두세요.

```
   예)  100.109.70.91
```

4부에서 태블릿에 이 주소를 심습니다. (자동으로 잡히지만, 안 될 때 직접 입력해야 합니다.)

---

# 3부. 관리자 PC 준비

## 3-1. 서버 준비 — 처음 한 번만

폴더의 **`서버-준비.bat`** 을 더블클릭하세요.

- 검은 창이 뜨고 여러 줄이 지나갑니다 (인터넷에서 부품을 받습니다. 1~3분)
- 마지막에 **`[OK] Server is ready.`** 가 보이면 성공
- 아무 키나 눌러 창을 닫습니다

> 실패한다면? 거의 항상 **Node.js 미설치**입니다 (1-1로).
>
> ❗ 이 단계는 **이 PC에서 직접** 해야 합니다. 다른 컴퓨터에서 만든 걸 복사해 오면
> 작동하지 않습니다(윈도우 전용 부품이 필요합니다).

## 3-2. 관리 서버 켜기

**`관리자-실행.bat`** 을 더블클릭 → 브라우저가 자동으로 열립니다.

검은 창에 이런 정보가 뜹니다:

```
  ->    주소: http://localhost:8090/dashboard
  ->    비밀번호: xxxxxxxx
  ->    태블릿용 주소(같은 와이파이): http://192.168.x.x:8090
  ->    태블릿용 주소(다른 망, NetBird): http://100.x.x.x:8090   ← 이게 2-5의 주소
```

- **비밀번호**는 `server\data\admin-password.txt` 파일에도 저장돼 있습니다
- 서버는 창을 닫아도 계속 돕니다. 끄려면 `관리자-종료.bat`

## 3-3. PC를 켤 때 서버도 자동으로 켜지게 (권장)

PC를 재부팅하면 서버가 자동으로 뜨지 않습니다. 자동 실행을 걸어두세요.

1. 키보드 **⊞ Windows + R** → `shell:startup` 입력 → 엔터 (폴더가 열립니다)
2. `관리자-실행.bat` 을 **마우스 오른쪽 버튼으로 드래그**해서 그 폴더에 놓고
   → 메뉴에서 **[바로 가기 만들기]** 선택

## 3-4. 대시보드 둘러보기

브라우저에 대시보드가 보이면 준비 완료입니다.

- 화면 오른쪽 위 **`?` 배지에 마우스를 올리면** — 원격 관리 원리 설명이 뜹니다
- **`넷버드 콘솔 ↗`** 버튼 — 넷버드 Peers 화면으로 바로 이동
- 탭 3개: **기기 현황** / **앱 배포** / **영상 자료실**

---

# 4부. 태블릿 세팅 (한 대마다 약 15분)

## 4-1. 태블릿 쪽 사전 준비

| 확인 | 방법 |
|---|---|
| **계정이 하나도 없어야 함** ★ | 설정 → 계정 → Google 계정 등 **전부 삭제**. 안 되면 공장초기화 |
| **USB 디버깅 켜기** | 설정 → 태블릿 정보 → **빌드번호를 7번 연속 탭** → 뒤로 → 개발자 옵션 → USB 디버깅 켜기 |
| **와이파이 연결** | 세팅 중에는 **관리자 PC와 같은 와이파이**에 연결 |
| **충전기 연결** | 세팅 중 배터리가 닳지 않게 |

> ★ 계정이 남아 있으면 키오스크 잠금이 **반드시 실패**합니다. 안드로이드 규칙이라
> 우회할 방법이 없습니다.

## 4-2. 세팅 실행

1. 태블릿을 **USB 케이블로 PC에 연결** (충전 전용 케이블 말고 **데이터 전송용**)
2. 태블릿 화면에 **"USB 디버깅을 허용하시겠습니까?"** → **[항상 허용] 체크 → [허용]**
3. 폴더의 **`태블릿-세팅.bat`** 더블클릭

이제 화면 안내를 따라가면 됩니다. 물어보는 것은 **딱 2개**입니다.

### ① 도서관 주소

```
  도서관 서브도메인 (전체 URL 붙여넣기도 가능, 비우면 앱 기본값 유지):
```
→ 그 도서관의 서브도메인만 입력. 예: **`splib`** → `https://splib.dobedub.com/home` 으로 설정됩니다.

### ② 관리 서버 주소 ★가장 중요

```
  이 PC 의 넷버드 주소를 찾았습니다: http://100.x.x.x:8090
  이 주소로 설정할까요? (엔터=예 / 다른 주소를 직접 입력)
```
→ **그냥 엔터**를 누르세요.

> ❗ **이 단계를 빠뜨리면 그 태블릿은 회사 대시보드에 나타나지 않습니다.**
> 앱에는 개발용 주소가 기본으로 들어 있어서, 여기서 덮어써야 회사 PC로 연결됩니다.
>
> 주소가 자동으로 안 잡히면 **2-5에서 메모한 주소**를 직접 입력하세요.
> (`http://100.x.x.x:8090` 형식 — 앞의 `http://` 와 뒤의 `:8090` 까지 그대로)

### ③ 나머지는 자동

WebView 업데이트 → 앱 설치 → 키오스크 잠금 → 화면 세로 고정 → **넷버드 설치·등록** →
불필요한 앱 정리 → 영상 넣기 → 검증

중간에 태블릿 화면에 **"VPN 연결을 허용하시겠습니까?"** 가 뜨면 **[확인]** 을 눌러주세요.
(자동으로 눌리지만, 안 눌리면 직접 눌러주세요.)

### ④ 성공 확인 — 이 3줄이 보여야 합니다

```
  [OK] Device Owner 지정 완료
  [OK] NetBird 등록 완료 — 태블릿 넷버드 IP: 100.x.x.x
  [OK] 관리 서버 주소를 기기에 설정: http://100.x.x.x:8090
```

케이블을 뽑아도 됩니다.

## 4-3. 세팅 확인

1. 관리 대시보드 **기기 현황** 탭에 방금 세팅한 태블릿이 나타나는지 확인
   - 최대 10분 걸릴 수 있습니다
   - **빨리 보고 싶으면**: 태블릿에서 홈 제목 5번 탭 → PIN `0000` → **업데이트 확인**
2. 기기 행에 **`VPN 미지정`** 경고가 **없어야** 정상입니다

## 4-4. 태블릿을 도서관에 설치할 때

- 태블릿은 **상시 충전 거치**를 권장합니다 (화면이 켜져 있으면 배터리가 빨리 닳습니다)
- 도서관 와이파이에만 연결해주면 끝입니다. **도서관에 별도 설정 요청은 필요 없습니다**

---

# 5부. 평소 운영

> 화면별 상세 조작은 **`PC관리자_사용법.md`** 에 있습니다. 여기서는 요약만.

## 5-1. 꼭 알아야 할 두 가지

### ① 지시는 "다음 접속 때" 실행됩니다

```
관리자에서 누름 → 대기 표시 → 태블릿이 접속 → 실행 → 완료 표시
                              (보통 몇 초 ~ 최대 10분)
```

### ② "미접속"은 고장이 아닙니다

태블릿 화면이 꺼져 있으면 절전 때문에 접속이 늦어집니다. **밤에는 아침까지 안 옵니다.**

| 표시 | 뜻 | 할 일 |
|---|---|---|
| 🟢 접속 중 | 정상 | 없음 |
| 🟡 미접속 | 절전 중 (정상) | 없음 |
| 🔴 연락 두절 | **24시간 넘게 소식 없음** | 이때만 확인 |

## 5-2. 자주 하는 일

| 하고 싶은 것 | 방법 |
|---|---|
| 영상 넣기 | `server\data\videos` 폴더에 mp4 복사 → **영상 자료실** 탭에 자동 등록 |
| 태블릿에 영상 보내기 | **기기 현황** → 그 기기의 **영상** 버튼 → 체크 → 보내기 |
| 영상 표지 바꾸기 | **영상 자료실** → **썸네일 등록/교체** |
| 홈 화면 안내 이미지 | **기기 현황** → **이미지** 버튼 |
| 앱 새 버전 배포 | **앱 배포** 탭 → APK 선택 + 버전 입력 → 업로드 (이후 자동 설치) |
| 태블릿 재부팅 | **기기 현황** → **설정** → 원격 재부팅 |
| 문의 전화번호 변경 | **기기 현황** → **설정** → 문의 연락처 |
| PIN 분실 | **기기 현황** → **설정** → PIN 초기화 (`0000` 복구) |

## 5-3. 태블릿 관리자 화면 열기

홈 화면 제목 **"보이스툰 도서관"을 5번 연속 탭** → PIN **`0000`**

여기서: 키오스크 해제/재진입, Wi-Fi 설정, 업데이트 확인, 기기 재부팅, 서버 주소 변경

## 5-4. 백업 (월 1회 권장)

`server\data` 폴더를 통째로 외장하드/클라우드에 복사하세요.
기기 목록·영상·안내 이미지가 전부 여기 있습니다.

---

# 6부. 문제가 생겼을 때

## 6-1. 증상별 해결

| 증상 | 원인과 해결 |
|---|---|
| 대시보드가 안 열림 | 서버가 꺼짐 → **`관리자-실행.bat`** 다시 실행 |
| 갑자기 전부 빨간색 | PC 재부팅으로 서버가 꺼짐 → `관리자-실행.bat` |
| **태블릿이 목록에 없음** | ① 4-2의 ②(관리 서버 주소)를 건너뛰었는지 확인 ② 태블릿 관리자 → 원격 관리 → **서버 자동 찾기** ③ 넷버드 콘솔 Peers 에 그 태블릿이 있는지 |
| **예전에 설치한 태블릿이 안 보임** | **정상입니다.** 0부의 "이미 나가 있는 태블릿들" 참조 — 회수해서 4부를 다시 실행해야 편입됩니다 |
| `VPN 미지정` 경고 | 그 태블릿은 **재부팅하면 원격이 끊깁니다.** 앱을 최신으로 업데이트하세요 |
| 다음 날 갑자기 다 끊김 | **관리 PC의 넷버드 로그인 만료.** 2-4의 ②(Login Expiration 끄기)를 안 했을 가능성. 태블릿 쪽 문제가 아닙니다 |
| 새 태블릿 등록이 안 됨 | 열쇠(Setup Key) 만료 가능성 → 2-5-1 참조. **이미 나간 태블릿은 영향 없습니다** |
| 영상을 보냈는데 안 감 | 태블릿이 접속해야 시작. 태블릿에서 **업데이트 확인**을 누르면 즉시 |
| 업데이트가 안 깔림 | 아이가 사용 중이면 미룹니다. 급하면 **알림 보내기** 또는 **즉시** |
| 태블릿 화면이 검게 멈춤 | **설정 → 원격 재부팅.** 접속이 끊겼으면 현장에서 전원 껐다 켜기 |
| 소리가 안 남 / 더빙 이상 | 앱 버전이 **2.5.3 이상**인지 확인 (이전 버전의 알려진 문제) |
| 세팅 중 Device Owner 실패 | 태블릿에 계정이 남아 있음 → 전부 삭제 후 재시도 (안 되면 공장초기화) |
| 세팅 중 `adb.exe 를 찾을 수 없습니다` | 1-2의 platform-tools 폴더가 없거나 위치가 틀림 |
| 세팅 중 넷버드 등록 건너뜀 | `netbird-*.apk` / `ADBKeyboard.apk` / `netbird-setup-key.txt` 중 빠진 것 확인 |

## 6-2. 넷버드가 이상할 때 확인 순서

1. **회사 PC**: 넷버드 앱이 **Connected** 인가? 아니면 Connect 클릭
2. **콘솔**: https://app.netbird.io/peers 에서 PC와 태블릿이 모두 보이는가?
3. **그룹**: PC = `fleet-server`, 태블릿 = `kiosk` 로 되어 있는가?
4. **만료**: PC의 Login Expiration 이 꺼져 있는가?

## 6-3. 담당자에게 연락할 때

아래 3가지를 함께 알려주시면 훨씬 빨리 해결됩니다:

1. 대시보드 **기기 현황** 화면 캡처
2. 그 태블릿의 **기관명**과 **마지막 접속** 시각
3. 무엇을 눌렀을 때 무슨 일이 일어났는지

---

# 부록 A. 폴더 안에 무엇이 있나

| 파일/폴더 | 용도 |
|---|---|
| **`서버-준비.bat`** | 처음 한 번 — 서버 부품 설치 |
| **`관리자-실행.bat`** | 관리 서버 켜기 (매번) |
| **`관리자-종료.bat`** | 관리 서버 끄기 |
| **`태블릿-세팅.bat`** | 태블릿 한 대 세팅 |
| `dobedub-kiosk-v2.5.5.apk` | 태블릿에 설치되는 앱 |
| `ADBKeyboard.apk` | 세팅 중 자동 입력용 (건드릴 필요 없음) |
| `server\` | 관리 서버 (자료는 `server\data` 에 쌓입니다) |
| `준비물-여기에-넣으세요.txt` | 1부 준비물 안내 (메모장으로 열림) |
| **`회사_인수인계_가이드.md`** | 이 문서 |
| `PC관리자_사용법.md` | 대시보드 화면별 상세 사용법 |
| `납품_매뉴얼.md` | 태블릿 세팅 상세 절차 |
| `원격관리_NetBird_도입.md` | 넷버드 구조 (기술 문서) |
| `원격관리_업데이트.md` | 자동 업데이트 원리, 서명 키 경고 (기술 문서) |
| `README.md` | 앱 개요 (기술 문서) |
| `setup-tablet.ps1` / `start-admin.ps1` / `provision-kiosk.ps1` | 위 .bat 들이 내부적으로 실행하는 파일. **직접 실행하지 마세요** |
| `app\` | 대시보드에서 기본 이용안내 이미지를 미리보기용으로 읽는 폴더 (건드리지 마세요) |

> `.md` 파일은 메모장으로도 열리지만, 웹브라우저나 **Visual Studio Code** 로 열면
> 표가 예쁘게 보입니다.

# 부록 B. 용어 사전

| 용어 | 쉬운 설명 |
|---|---|
| **넷버드 (NetBird)** | 회사 PC와 태블릿을 보이지 않는 하나의 사내망으로 묶는 프로그램 |
| **Peer(피어)** | 그 사내망에 등록된 기기 한 대 |
| **Setup Key(셋업 키)** | 태블릿을 사람 로그인 없이 자동 등록시키는 열쇠 문자열 |
| **그룹 (Group)** | 기기 묶음. `kiosk`=태블릿들, `fleet-server`=관리 PC |
| **adb** | PC가 USB로 안드로이드 기기와 대화하는 도구 |
| **APK** | 안드로이드 앱 설치 파일 (윈도우의 .exe 같은 것) |
| **Device Owner** | 안드로이드가 인정하는 "이 기기의 주인 앱". 키오스크 잠금의 근거 |
| **체크인** | 태블릿이 관리 서버에 스스로 접속해 상태를 보고하고 지시를 받아오는 것 |
| **대시보드** | 매일 보는 관리 화면 (http://localhost:8090/dashboard) |
| **versionCode** | 앱 버전 번호(숫자). 이 숫자가 커야 태블릿이 업데이트를 받습니다 |

# 부록 C. 개발 담당자용 참고

- 이 버전은 **v2.5.5 / versionCode 32** 입니다. 새 APK 배포 시 versionCode는
  **반드시 이전보다 큰 숫자**여야 합니다.
- **서명 키(`release-keystore.jks`)를 잃으면 배포된 태블릿을 다시는 업데이트할 수 없습니다**
  (기기마다 공장초기화 필요). 별도 백업 여부를 확인하세요.
- 개발용 서버(맥)와 이 운영 PC는 **서로 다른 함대**입니다. 태블릿은 세팅 때 심은 주소로만
  접속하므로 섞이지 않습니다. 개발용 태블릿 1대는 계속 개발 서버에 붙어 있습니다.
- 넷버드 무료 플랜: **사용자 5명 / 기기 100대**. 현재 사용량은 콘솔 왼쪽 아래에 표시됩니다.
- 접속 규칙은 https://app.netbird.io/access-control 에서 `kiosk → fleet-server:8090` 하나만
  열려 있습니다. 기기가 늘어도 이 규칙은 그대로 두면 됩니다.
