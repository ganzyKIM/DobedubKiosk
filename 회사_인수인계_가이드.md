# 두비덥 보이스툰 키오스크 — 회사 인수인계 가이드 (윈도우 PC용)

이 문서 하나만 따라 하면 **관리자 PC 준비 → 태블릿 세팅 → 평소 운영**까지 끝납니다.
컴퓨터에 익숙하지 않아도 되도록, 누를 것과 보일 화면을 그대로 적었습니다.

> **읽는 순서**
> 1부 = 처음 한 번만 하는 준비 (30분)
> 2부 = 태블릿 한 대 세팅 (대당 15분)
> 3부 = 평소 운영 (매일 쓰는 것)
> 4부 = 문제가 생겼을 때
>
> 급하면 **1부 → 2부**만 하면 태블릿이 돌아갑니다.

---

# 0. 이게 무엇인가 (1분)

태블릿을 **딱 3가지만 되는 키오스크**로 잠그는 시스템입니다.

| 태블릿 화면 | 내용 |
|---|---|
| 🎬 **동영상** | 태블릿에 넣어둔 영상 재생 |
| 📖 **도서관** | 보이스툰 웹툰 보기 |
| 🎤 **마이보이스** | 아이가 직접 목소리를 입히는 더빙 |

아이는 이 3개 밖으로 나갈 수 없습니다(홈 버튼·다른 앱·인터넷 주소창 전부 차단).

그리고 **관리자 PC 한 대**가 전국 도서관의 태블릿들을 원격으로 관리합니다 —
영상 보내기, 앱 업데이트, 상태 확인, 재부팅까지 **현장에 가지 않고** 합니다.

```
   [회사 관리자 PC]  ←── 인터넷(넷버드 전용망) ──→  [도서관 태블릿들]
    · 대시보드로 상태 확인                            · 10분마다 접속해서
    · 영상/업데이트 보내기                              지시를 받아감
```

> **넷버드(NetBird)** 는 회사 PC와 태블릿을 하나의 안전한 가상 전용망으로 묶어주는
> 프로그램입니다. 이것 덕분에 태블릿이 **어느 도서관 와이파이에 있든** 관리가 됩니다.
> (도서관에 별도 설정을 요청할 필요가 없습니다.)

---

# 1부. 관리자 PC 준비 — 처음 한 번만 (약 30분)

## 1-1. 필요한 것 확인

| 준비물 | 설명 |
|---|---|
| 윈도우 PC | **항상 켜둘 수 있는 PC.** 이 PC가 꺼지면 원격 관리가 멈춥니다 |
| 인터넷 | 유선/무선 무관 |
| 이 폴더 | 압축을 풀어둔 `두비덥키오스크_운영_v2.5.5` 폴더 |
| USB 케이블 | 태블릿 세팅용 (데이터 전송용. 충전 전용 케이블은 안 됩니다) |

## 1-2. Node.js 설치 (서버 프로그램을 돌리는 엔진)

1. https://nodejs.org 접속
2. 왼쪽의 **LTS** 버튼 클릭 → 다운로드
3. 받은 파일 실행 → **계속 [다음]** 만 누르면 됩니다 (설정 바꿀 것 없음)

> 버전을 고민하지 마세요. 사이트가 권하는 LTS 를 그대로 받으면 됩니다.

## 1-3. 넷버드 설치 — 원격 관리의 핵심

이 단계를 해야 **도서관에 나간 태블릿**을 관리할 수 있습니다.

1. https://app.netbird.io 접속 → 회사 계정으로 로그인
   - 계정: **devops@dobedub.com** (비밀번호는 담당자에게 확인)
2. 로그인 후 왼쪽 메뉴 **Peers** → 오른쪽 위 **Add Peer** 클릭
3. **Windows** 탭 선택 → 안내에 따라 설치 파일 다운로드 → 설치
4. 설치가 끝나면 넷버드 창에서 **Connect** (또는 로그인) — 같은 계정으로 로그인
5. 연결되면 이 PC에 **100.으로 시작하는 주소**가 생깁니다. 이게 태블릿들이 찾아올 주소입니다.

### ⚠ 반드시 해야 하는 설정 — 자동 로그아웃 끄기

넷버드는 기본적으로 **하루가 지나면 로그인이 풀립니다.** 관리자 PC가 그렇게 되면
다음 날 원격 관리가 끊깁니다. 그래서 이 PC만 예외로 지정합니다.

1. https://app.netbird.io → **Peers**
2. 방금 추가된 이 PC 이름 클릭
3. **Login Expiration** (로그인 만료) 항목을 **끄기(Disabled)**
4. 저장

> 태블릿은 이 설정을 건드리지 않아도 됩니다 — 태블릿은 setup key로 등록되어
> 만료가 적용되지 않습니다.

### 그룹 확인 (한 번만)

넷버드 콘솔에서 이 PC가 **`fleet-server`** 그룹에 들어 있는지 확인하세요.
없다면 PC 이름 클릭 → Groups 에서 `fleet-server` 추가 → 저장.

> 왜 필요한가: 태블릿(`kiosk` 그룹)은 **`fleet-server` 그룹의 8090 포트로만** 접속하도록
> 제한돼 있습니다. 그룹이 안 맞으면 태블릿이 이 PC를 못 찾습니다.

## 1-4. 서버 준비 — 처음 한 번만

폴더 안의 **`서버-준비.bat`** 을 더블클릭하세요.

- 검은 창이 뜨고 여러 줄이 지나갑니다 (인터넷에서 부품을 받아옵니다. 1~3분)
- 마지막에 **`[OK] Server is ready.`** 가 보이면 성공입니다
- 아무 키나 눌러 창을 닫습니다

> ❗ 이 단계는 **이 PC에서 직접** 해야 합니다. 다른 컴퓨터에서 만든 걸 복사해 오면
> 작동하지 않습니다(윈도우 전용 부품이 필요합니다).
>
> 만약 빨간 글씨로 실패하면 → Node.js 설치를 건너뛰었을 가능성이 큽니다. 1-2로 돌아가세요.

## 1-5. 관리 서버 켜기

**`관리자-실행.bat`** 을 더블클릭하세요. 브라우저가 자동으로 열립니다.

```
  ->    주소: http://localhost:8090/dashboard
  ->    비밀번호: (화면에 표시됩니다)
  ->    태블릿용 주소(다른 망, NetBird): http://100.x.x.x:8090   ← 이걸 적어두세요
```

- **비밀번호**는 `server\data\admin-password.txt` 파일에도 저장돼 있습니다
- **"태블릿용 주소(다른 망, NetBird)"** 를 메모해 두세요. 2부에서 씁니다
- 서버는 창을 닫아도 계속 돕니다. 끄려면 `관리자-종료.bat`

### PC를 켤 때 서버도 자동으로 켜지게 하기 (권장)

PC를 재부팅하면 서버가 자동으로 뜨지 않습니다. 자동 실행을 걸어두세요:

1. 키보드 **⊞ Windows + R** → `shell:startup` 입력 → 엔터 (시작프로그램 폴더가 열림)
2. `관리자-실행.bat` 을 **오른쪽 드래그**해서 이 폴더에 놓고 → **바로 가기 만들기** 선택

이제 PC를 켜면 관리 서버도 함께 켜집니다.

## 1-6. 준비 완료 확인

브라우저에서 대시보드가 보이면 성공입니다. 화면 오른쪽 위:

- **`?` 배지에 마우스를 올리면** — 원격 관리 원리 설명이 나옵니다
- **`넷버드 콘솔 ↗`** — 태블릿 등록 상태를 보는 넷버드 화면으로 이동

---

# 2부. 태블릿 세팅 — 태블릿 한 대마다 (약 15분)

## 2-1. 먼저 준비할 것

폴더 안에 아래 3개가 **함께 있어야** 넷버드 자동 등록이 됩니다:

| 파일 | 구하는 곳 |
|---|---|
| `netbird-*.apk` | 담당자에게 받기 (약 200MB) |
| `ADBKeyboard.apk` | 담당자에게 받기 (작음) |
| `netbird-setup-key.txt` | **넷버드 콘솔 → Setup Keys → `kiosk-fleet` 키를 복사해 이 이름으로 저장** |

> 이 3개가 없으면 세팅은 진행되지만 넷버드 등록만 건너뜁니다(나중에 수동으로 해야 함).
>
> 🔐 `netbird-setup-key.txt` 는 **비밀번호와 같습니다.** 메일·메신저로 돌리지 마세요.
> 유출되면 넷버드 콘솔에서 그 키를 **Revoke(폐기)** 하고 새로 만드세요.

### 태블릿 쪽 사전 준비 (중요)

| 확인 | 방법 |
|---|---|
| **계정이 하나도 없어야 함** | 설정 → 계정. Google 계정 등이 있으면 **전부 삭제**. 안 지워지면 공장초기화 |
| **USB 디버깅 켜기** | 설정 → 태블릿 정보 → **빌드번호 7회 연속 탭** → 개발자 옵션 → USB 디버깅 켜기 |
| 와이파이 연결 | 세팅 중에는 **관리자 PC와 같은 와이파이**에 연결 |

> 계정이 남아 있으면 키오스크 잠금(Device Owner) 지정이 **반드시 실패**합니다.
> 이건 안드로이드의 규칙이라 우회할 수 없습니다.

## 2-2. 세팅 실행

1. 태블릿을 **USB 케이블로 PC에 연결**
2. 태블릿 화면에 **"USB 디버깅을 허용하시겠습니까?"** → **[항상 허용] 체크 후 [허용]**
3. 폴더의 **`태블릿-세팅.bat`** 더블클릭

이제 화면 안내를 따라가면 됩니다. 순서대로 이런 걸 묻습니다:

### ① 도서관 주소

```
  도서관 서브도메인 (전체 URL 붙여넣기도 가능, 비우면 앱 기본값 유지):
```
→ 그 도서관의 서브도메인만 입력합니다. 예: **`splib`** 를 넣으면
`https://splib.dobedub.com/home` 으로 설정됩니다.

### ② 관리 서버 주소 ★가장 중요

```
  이 PC 의 넷버드 주소를 찾았습니다: http://100.x.x.x:8090
  이 주소로 설정할까요? (엔터=예 / 다른 주소를 직접 입력)
```
→ **그냥 엔터**를 누르세요. 자동으로 찾은 이 PC의 넷버드 주소가 태블릿에 심어집니다.

> ❗ **이 단계를 건너뛰면 그 태블릿은 회사 대시보드에 나타나지 않습니다.**
> 앱에는 개발용 서버 주소가 기본으로 들어 있어서, 덮어쓰지 않으면 개발 서버로 붙습니다.
> 주소가 자동으로 안 잡히면 1-5에서 메모해 둔 주소를 직접 입력하세요.

### ③ 나머지는 자동

WebView 업데이트 → 앱 설치 → 키오스크 잠금 → **화면 세로 고정** →
**넷버드 설치·등록** → 불필요한 앱 정리 → 영상 넣기 → 검증

중간에 태블릿 화면에서 **VPN 연결을 허용하시겠습니까?** 가 뜨면 **[확인]** 을 눌러주세요.

### ④ 마지막 확인

```
  [OK] Device Owner 지정 완료
  [OK] NetBird 등록 완료 — 태블릿 넷버드 IP: 100.x.x.x
  [OK] 관리 서버 주소를 기기에 설정: http://100.x.x.x:8090
```

이 3줄이 보이면 성공입니다. 케이블을 뽑아도 됩니다.

## 2-3. 세팅 확인

1. 관리자 PC 대시보드를 새로고침
2. **기기 현황** 탭에 방금 세팅한 태블릿이 나타나면 완료
   (최대 10분 걸릴 수 있습니다. 태블릿 관리자 메뉴에서 **업데이트 확인**을 누르면 즉시 나타납니다)
3. 기기 행에 **`VPN 미지정`** 경고가 **없어야** 정상입니다

---

# 3부. 평소 운영

> 자세한 화면 조작은 **`PC관리자_사용법.md`** 에 그림처럼 적혀 있습니다.
> 여기서는 자주 쓰는 것만 요약합니다.

## 3-1. 꼭 알아야 할 두 가지

### ① 지시는 즉시가 아니라 "다음 접속 때" 실행됩니다

```
관리자에서 누름 → 대기 표시 → 태블릿이 접속 → 실행 → 완료 표시
                              (보통 몇 초 ~ 최대 10분)
```

### ② "미접속"은 고장이 아닙니다

태블릿 화면이 꺼져 있으면 절전 때문에 접속이 늦어집니다. **밤에는 아침까지 안 옵니다.**

| 표시 | 뜻 | 할 일 |
|---|---|---|
| 🟢 접속 중 | 정상 | 없음 |
| 🟡 미접속 | 절전 중 (정상) | 없음 |
| 🔴 연락 두절 | **24시간 넘게 소식 없음** | 이때만 확인 |

## 3-2. 자주 하는 일

| 하고 싶은 것 | 방법 |
|---|---|
| 영상 넣기 | `server\data\videos` 폴더에 mp4 복사 → 대시보드 **영상 자료실** 탭에 자동 등록 |
| 태블릿에 영상 보내기 | **기기 현황** 탭 → 그 기기의 **영상** 버튼 → 체크 → 보내기 |
| 영상 표지 바꾸기 | **영상 자료실** 탭 → **썸네일 등록/교체** |
| 홈 화면 안내 이미지 | **기기 현황** → **이미지** 버튼 |
| 앱 새 버전 배포 | **앱 배포** 탭 → APK 선택 + 버전 입력 → 업로드 (이후 자동 설치) |
| 태블릿 재부팅 | **기기 현황** → **설정** → 원격 재부팅 |
| 문의 전화번호 변경 | **기기 현황** → **설정** → 문의 연락처 |
| PIN 분실 | **기기 현황** → **설정** → PIN 초기화 (`0000` 으로 복구) |

## 3-3. 태블릿 관리자 화면 열기

홈 화면 제목 **"보이스툰 도서관"을 5번 연속 탭** → PIN **`0000`**

여기서: 키오스크 해제/재진입, Wi-Fi 설정, 업데이트 확인, 기기 재부팅, 서버 주소 변경

## 3-4. 백업 (월 1회 권장)

`server\data` 폴더를 통째로 다른 위치(외장하드/클라우드)에 복사하세요.
기기 목록·영상·안내 이미지가 모두 여기 있습니다.

---

# 4부. 문제가 생겼을 때

| 증상 | 원인과 해결 |
|---|---|
| 대시보드가 안 열림 | 서버가 꺼진 것. **`관리자-실행.bat`** 다시 실행 |
| 태블릿이 목록에 없음 | ① 2-2의 ②단계(관리 서버 주소)를 건너뛰었는지 확인 ② 태블릿 관리자 메뉴 → 원격 관리 → **서버 자동 찾기** |
| 갑자기 전부 빨간색 | PC 재부팅으로 서버가 꺼짐 → `관리자-실행.bat` |
| `VPN 미지정` 경고 | 그 태블릿은 **재부팅하면 원격이 끊깁니다.** 앱을 최신 버전으로 업데이트하세요 |
| 영상을 보냈는데 안 감 | 태블릿이 접속해야 시작됩니다. 태블릿에서 **업데이트 확인**을 누르면 즉시 |
| 업데이트가 안 깔림 | 아이가 사용 중이면 방해하지 않고 미룹니다. 급하면 **알림 보내기** 또는 **즉시** |
| 태블릿 화면이 검게 멈춤 | **설정 → 원격 재부팅.** 접속이 끊긴 상태면 현장에서 전원 껐다 켜기 |
| 소리가 안 남 / 더빙이 이상함 | 앱 버전이 **2.5.3 이상**인지 확인 (그 이전 버전의 알려진 문제) |
| 세팅 중 Device Owner 실패 | 태블릿에 계정이 남아 있는 것. 전부 삭제 후 재시도 (안 되면 공장초기화) |

## 그래도 안 될 때

담당자에게 연락할 때 **아래 3가지**를 알려주시면 훨씬 빨리 해결됩니다:

1. 대시보드 **기기 현황** 화면 캡처
2. 그 태블릿의 **기관명**과 **마지막 접속** 시각
3. 무엇을 눌렀을 때 무슨 일이 일어났는지

---

# 부록 A. 폴더 안에 무엇이 있나

| 파일/폴더 | 용도 |
|---|---|
| **`서버-준비.bat`** | 처음 한 번 — 서버 부품 설치 |
| **`관리자-실행.bat`** | 관리 서버 켜기 (매번) |
| **`관리자-종료.bat`** | 관리 서버 끄기 |
| **`태블릿-세팅.bat`** | 태블릿 한 대 세팅 |
| `dobedub-kiosk-v2.5.5.apk` | 태블릿에 설치되는 앱 |
| `server\` | 관리 서버 프로그램 (`server\data` 안에 자료가 쌓임) |
| **`회사_인수인계_가이드.md`** | 이 문서 |
| `PC관리자_사용법.md` | 대시보드 화면별 상세 사용법 |
| `납품_매뉴얼.md` | 태블릿 세팅 상세 절차 |
| `원격관리_NetBird_도입.md` | 넷버드 구조·등록 절차 (기술 문서) |
| `README.md` / `CLAUDE.md` | 개발·기술 배경 (평소엔 볼 일 없음) |

> `.md` 파일은 메모장으로도 열리지만, **Visual Studio Code** 나 웹브라우저로 보면
> 표가 예쁘게 보입니다.

# 부록 B. 개발 담당자만 아는 것 (참고)

- 이 버전은 **v2.5.5 / versionCode 32** 입니다. 앱을 새로 배포할 때 versionCode는
  **반드시 이전보다 큰 숫자**여야 태블릿이 업데이트를 받습니다.
- **서명 키(`release-keystore.jks`)를 잃어버리면 이미 배포된 태블릿을 다시는 업데이트할 수
  없습니다.** 기기마다 공장초기화가 필요해집니다. 개발 담당자가 별도 백업 중인지 확인하세요.
- 개발용 서버(맥)와 이 운영 PC는 **서로 다른 함대**입니다. 태블릿은 세팅 때 심은 주소로만
  접속하므로 섞이지 않습니다. 개발용 태블릿 1대는 계속 개발 서버에 붙어 있습니다.
- 넷버드 무료 플랜: 사용자 5명 / 기기 100대까지. 현재 사용량은 콘솔에서 확인 가능합니다.
