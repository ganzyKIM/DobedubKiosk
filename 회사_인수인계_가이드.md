# 두비덥 보이스툰 키오스크 — 인수인계 가이드 (윈도우 PC)

관리자 PC 준비부터 태블릿 세팅, 평소 운영까지 이 문서 하나로 진행합니다.
0~4부는 순서대로 해야 합니다.

| 부 | 내용 | 소요 |
|---|---|---|
| 0부 | 시스템 개요 | 읽기 3분 |
| 1부 | 준비물 내려받기 | 20분 |
| 2부 | 넷버드 계정·설정 | 20분 |
| 3부 | 관리자 PC 준비 | 15분 |
| 4부 | 태블릿 세팅 | 대당 15분 |
| 5부 | 평소 운영 | — |
| 6부 | 문제 해결 | — |

---

# 0부. 시스템 개요

## 태블릿

태블릿은 세 가지 기능만 동작하는 키오스크로 잠깁니다.

| 버튼 | 기능 |
|---|---|
| 동영상 | 태블릿에 넣어둔 영상 재생 |
| 도서관 | 보이스툰 웹툰 보기 |
| 마이보이스 | 아이가 목소리를 입히는 더빙 |

홈 버튼, 다른 앱, 주소창은 모두 차단되어 있어 이 밖으로 나갈 수 없습니다.

## 관리자 PC

회사 PC 한 대가 전국 도서관의 태블릿을 원격으로 관리합니다.
영상 전송, 앱 업데이트, 상태 확인, 재부팅을 현장 방문 없이 처리합니다.

```
[회사 관리자 PC] ←─ 넷버드 전용망 ─→ [도서관 태블릿들]
 상태 확인, 지시 등록              10분마다 접속해 지시 수행
```

## 넷버드(NetBird)가 필요한 이유

도서관 태블릿은 각자 그 도서관의 와이파이를 씁니다. 회사 PC와는 다른 네트워크라
원래는 서로 연결할 수 없고, 도서관마다 공유기 설정을 요청할 수도 없습니다.

넷버드는 회사 PC와 태블릿들을 하나의 가상 사내망으로 묶는 프로그램입니다.
설치하면 기기마다 100.으로 시작하는 고정 주소가 생기고, 태블릿은 어느 와이파이에
있든 이 주소로 회사 PC에 접속합니다. 통신은 전 구간 암호화되며(WireGuard),
태블릿은 회사 PC의 관리 포트(8090) 하나로만 접속하도록 제한되어 있습니다.

## 화면 세 가지 구분

| 이름 | 용도 | 사용 빈도 |
|---|---|---|
| 넷버드 콘솔 (웹) | 기기 등록·연결 상태 확인 | 가끔 |
| 관리 대시보드 (웹) | 태블릿 상태·영상·업데이트. 실제 운영 화면 | 매일 |
| 태블릿 관리자 화면 | 태블릿에서 여는 숨은 설정 | 현장에서 가끔 |

## 주의 — 이미 나가 있는 태블릿 (군산 등)

이 시스템 도입 전에 설치된 태블릿들은 예전 관리자 PC를 바라보고 있습니다.
관리자가 회사 PC로 바뀌면 그 연결은 끊깁니다.

| | 새로 세팅한 태블릿 | 기존 태블릿 |
|---|---|---|
| 대시보드 표시 | 표시됨 | 표시 안 됨 |
| 원격 영상·업데이트 | 가능 | 불가 |
| 태블릿 자체 동작 | 정상 | 정상 |

기존 태블릿이 대시보드에 안 보이는 것은 정상입니다. 고장이 아니며 아이들은
그대로 이용할 수 있습니다. 원격 관리만 안 되는 상태입니다.

원격으로는 복구할 수 없습니다(지시가 전달될 경로 자체가 끊겨 있으므로).
회수해서 다시 세팅하는 것이 유일한 방법입니다.

1. 태블릿을 회사로 회수
2. 이 가이드 4부를 다시 실행
3. 대시보드에 나타나는지 확인 후 재설치

한 번만 하면 되고 이후 회수할 일은 없습니다. 급하지 않으니 도서관 방문 일정에
맞춰 처리해도 됩니다.

---

# 1부. 준비물 내려받기

받은 파일은 모두 `DobedubKiosk-Operations` 폴더 안에 넣습니다.

| # | 준비물 | 용도 | 출처 |
|---|---|---|---|
| 1 | Node.js | 관리 서버 실행 엔진 | https://nodejs.org |
| 2 | platform-tools | 태블릿 USB 연결 도구(adb) | https://developer.android.com/tools/releases/platform-tools |
| 3 | 넷버드 윈도우용 | 회사 PC를 전용망에 연결 | 2부에서 설치 |
| 4 | 넷버드 안드로이드 APK | 태블릿을 전용망에 연결 | https://github.com/netbirdio/android-client/releases |
| 5 | ADBKeyboard.apk | 세팅 중 자동 입력용 | 패키지에 포함되어 있음 |

## 1-1. Node.js

1. https://nodejs.org 에서 LTS 버튼을 눌러 내려받기
2. 실행 후 [Next]만 계속 눌러 설치 (설정 변경 불필요)

버전은 사이트가 권하는 LTS를 그대로 받으면 됩니다.

## 1-2. platform-tools (adb)

설치 프로그램이 아니라 압축을 풀어 폴더에 넣는 방식입니다.

1. https://developer.android.com/tools/releases/platform-tools 접속
2. "Download SDK Platform-Tools for Windows" 클릭 → 약관 동의 → 다운로드 (약 15MB)
3. 압축을 풀면 `platform-tools` 폴더가 나옴
4. 그 폴더를 통째로 `DobedubKiosk-Operations` 안에 넣기

```
DobedubKiosk-Operations\
├── platform-tools\        ← 폴더 이름 그대로
│   └── adb.exe
├── 태블릿-세팅.bat
└── ...
```

## 1-3. 넷버드 안드로이드 APK

태블릿용 넷버드입니다. 플레이스토어를 쓸 수 없는 기기라 파일로 설치합니다.

1. https://github.com/netbirdio/android-client/releases 접속
2. 맨 위 릴리스의 Assets를 펼쳐 `.apk` 파일 다운로드 (약 200MB)
3. `DobedubKiosk-Operations` 폴더에 넣기. 파일명은 `netbird-` 로 시작해야 함
   (예: `netbird-v0.5.0.apk`)

검증된 버전은 v0.5.0입니다. 최신 버전은 동작 확인이 안 되어 있으므로 v0.5.0을 권장합니다.

## 1-4. 확인

```
DobedubKiosk-Operations\
├── platform-tools\
├── netbird-v0.5.0.apk
├── ADBKeyboard.apk
├── netbird-setup-key.txt   ← 2부에서 생성
├── 서버-준비.bat
├── 관리자-실행.bat
├── 태블릿-세팅.bat
└── ...
```

---

# 2부. 넷버드 계정·설정

## 2-1. 로그인

- 계정: devops@dobedub.com (비밀번호는 담당자에게 확인)
- https://app.netbird.io 접속 → 로그인 → Peers 화면이 나오면 정상

## 2-2. 콘솔 주요 화면

| 화면 | 주소 | 용도 |
|---|---|---|
| Peers | https://app.netbird.io/peers | 등록된 기기 목록 |
| Setup Keys | https://app.netbird.io/setup-keys | 태블릿 자동 등록용 키 |
| Access Control | https://app.netbird.io/access-control | 접속 규칙 |

## 2-3. 태블릿 등록용 Setup Key

태블릿은 사람이 로그인하지 않고 키 문자열 하나로 자동 등록합니다.

1. https://app.netbird.io/setup-keys 접속
2. `kiosk-fleet` 키가 있고 값을 알고 있으면 그대로 사용
3. 없거나 값을 모르면 새로 생성:
   - Create Setup Key → Name: `kiosk-fleet`
   - Usage: Reusable
   - Expires: 1년 등 넉넉히
   - Auto-assign groups: `kiosk` — 반드시 지정
   - 생성 직후 표시되는 문자열을 복사 (이후 다시 볼 수 없음)
4. 메모장에 붙여넣어 `netbird-setup-key.txt` 이름으로
   `DobedubKiosk-Operations` 폴더에 저장

주의:
- 이 파일은 비밀번호에 준합니다. 메일·메신저로 보내지 마세요.
  유출 시 콘솔에서 해당 키를 Revoke하고 새로 만들면 됩니다.
- Auto-assign groups에 `kiosk`가 빠지면 태블릿이 등록은 되지만 회사 PC에
  접속 권한이 없어 대시보드에 나타나지 않습니다.

## 2-4. 관리자 PC를 전용망에 연결

1. https://app.netbird.io/peers → Add Peer → Windows → 설치 파일 다운로드 후 설치
2. 넷버드 앱에서 Connect → 브라우저가 열리면 같은 계정으로 로그인
3. Connected 표시 확인, Peers 목록에 이 PC가 나타나는지 확인

### 필수 설정 두 가지

빠뜨리면 다음 날 원격 관리가 끊깁니다.

1. **그룹 지정** — Peers에서 이 PC 클릭 → Groups에 `fleet-server` 추가 → 저장.
   태블릿(`kiosk` 그룹)은 `fleet-server` 그룹에만 접속하도록 규칙이 걸려 있습니다.
2. **Login Expiration 끄기** — Peers에서 이 PC 클릭 → Login Expiration을
   Disabled로 변경 → 저장. 넷버드는 기본적으로 24시간 뒤 로그인이 풀리는데,
   24시간 돌아야 하는 관리 PC에는 맞지 않습니다.

## 2-5. 키 만료에 대해

Setup Key는 등록 시점에만 사용됩니다. 한 번 등록된 태블릿은 키와 무관하게
동작하므로, 키가 만료되어도 운영 중인 태블릿은 끊기지 않습니다.
키가 만료되면 새 태블릿 등록만 안 되며, 그때 새 키를 만들면 됩니다
(위 2-3 절차, 옛 키는 Revoke로 정리).

실측: 2026-08-15에 이 키로 등록한 개발용 태블릿이 재인증 없이 계속
연결 유지 중입니다(8-31 기준 16일째).

만료로 끊기는 쪽은 태블릿이 아니라 관리 PC입니다(사람 계정 로그인 방식이므로).
2-4의 Login Expiration 끄기가 그 대비책입니다.

| 대상 | 등록 방식 | 만료 |
|---|---|---|
| 관리 PC | 계정 로그인(SSO) | 24시간 뒤 풀림 → 2-4에서 꺼야 함 |
| 태블릿 | Setup Key | 풀리지 않음 |

## 2-6. 이 PC의 전용 주소 메모

넷버드 앱 또는 Peers 목록에서 이 PC의 100.으로 시작하는 주소를 확인해 적어두세요.
예: `100.109.70.91`. 4부에서 태블릿에 이 주소를 입력하게 됩니다.

---

# 3부. 관리자 PC 준비

## 3-0. 예전 관리자가 이미 설치된 PC라면

원격 도입 전 버전(같은 와이파이 안에서만 쓰던 관리자)이 깔린 PC에 그대로 설치할 수 있습니다.
압축은 바탕화면 등 아무 곳에 풀어도 됩니다. 모든 스크립트는 자기 폴더를 기준으로 동작하고
폴더 이름이 예전 것과 다르므로 나란히 있어도 됩니다. 다만 순서가 있습니다.

1. 예전 서버 종료. 새 폴더의 `관리자-종료.bat` 을 실행합니다. 관리 포트(8090)를 쓰고 있는
   서버를 찾아 종료하므로 예전 것도 함께 꺼집니다. 이 단계를 건너뛰면 `관리자-실행.bat` 이
   "이미 실행 중입니다" 라고만 하고 브라우저에 예전 화면을 엽니다.
2. 시작프로그램 정리. Windows 키 + R → `shell:startup` 에 예전 실행 바로가기가 있으면
   삭제합니다. 남겨 두면 재부팅 때 예전 서버가 먼저 떠서 새 서버가 뜨지 못합니다.
3. 자료 이어받기(선택). 영상 자료실, 이용안내 이미지, 등록된 기기 목록을 그대로 쓰려면
   예전 폴더의 `server\data` 폴더를 새 폴더의 `server\` 안으로 복사합니다. 새 폴더에는
   `data` 가 없습니다. 첫 실행 때 자동으로 새 형식으로 갱신되며, 관리자 비밀번호도
   예전 것이 유지됩니다(`server\data\admin-password.txt`). 예전에 등록된 태블릿은
   미접속으로 표시되다가 회수해 4부 절차로 다시 세팅하면 같은 줄에서 이어집니다.
   복사하지 않으면 빈 상태로 시작하고 비밀번호는 3-2에서 새로 표시됩니다.
4. `platform-tools` 폴더는 예전 폴더 것을 새 폴더 옆에 복사해도 됩니다(1-2 참고).

이후 3-1부터 진행합니다.

**예전 폴더 정리**

새 관리자는 예전 폴더의 어떤 파일도 읽지 않습니다. 구버전 태블릿을 회수해 다시 세팅할 때도
예전 관리자는 필요 없습니다(4부의 세팅은 기기를 처음부터 다시 잡습니다). 예전 폴더는 새 관리자가
정상 동작하는 것을 확인한 뒤 지워도 되고, 안전을 위해 남겨 두어도 됩니다.

- 지울 때: 위 1번(서버 종료)과 3번(자료 복사)을 먼저 합니다. 영상 자료실의 영상과 이용안내
  이미지는 예전 `server\data` 안에만 있어서, 복사 없이 지우면 다시 올려야 합니다.
- 남길 때: 폴더 이름 뒤에 `_구버전` 을 붙이고 시작프로그램의 예전 바로가기를 지웁니다.
  누군가 예전 서버를 켜면 8090 포트를 차지해 새 관리자가 뜨지 못합니다.
- 지우면 안 되는 폴더: 안에 `release-keystore.jks` 또는 `keystore.properties` 가 있는 폴더는
  앱 소스 개발용입니다. 이 서명 키가 없으면 설치된 태블릿에 앱 업데이트를 보낼 수 없습니다.
  다른 곳에 백업이 있는지 확인하기 전에는 지우지 않습니다. 관리자 실행용 폴더에는 이 파일이
  없습니다.

## 3-1. 서버 준비 (최초 1회)

`서버-준비.bat` 더블클릭. 인터넷에서 서버 구성요소를 받아옵니다(1~3분).
마지막에 `[OK] Server is ready.` 가 나오면 완료입니다.

- 실패하는 경우 대부분 Node.js 미설치입니다(1-1 확인).
- 이 단계는 반드시 이 PC에서 실행해야 합니다. 다른 컴퓨터에서 만든 결과물을
  복사해 오면 동작하지 않습니다.

## 3-2. 관리 서버 실행

`관리자-실행.bat` 더블클릭. 브라우저에 대시보드가 열립니다.

```
->  주소: http://localhost:8090/dashboard
->  비밀번호: (창에 표시. server\data\admin-password.txt 에도 저장됨)
->  태블릿용 주소(다른 망, NetBird): http://100.x.x.x:8090   ← 2-6의 주소
```

- 창을 닫아도 서버는 계속 동작합니다. 종료는 `관리자-종료.bat`.

## 3-3. PC 부팅 시 자동 실행 등록 (권장)

PC를 재부팅하면 서버가 자동으로 뜨지 않으므로 시작프로그램에 등록해 둡니다.

1. Windows 키 + R → `shell:startup` 입력 → 엔터
2. 열린 폴더에 `관리자-실행.bat` 을 오른쪽 버튼으로 끌어다 놓고
   [바로 가기 만들기] 선택

## 3-4. 대시보드 확인

- 오른쪽 위 `?` 배지에 마우스를 올리면 원격 관리 원리 설명이 표시됩니다.
- `넷버드 콘솔` 버튼은 넷버드 Peers 화면을 새 탭으로 엽니다.
- 탭은 기기 현황 / 앱 배포 / 영상 자료실 세 개입니다.

---

# 4부. 태블릿 세팅

## 4-1. 태블릿 사전 준비

| 항목 | 방법 |
|---|---|
| 계정 전부 삭제 | 설정 → 계정. Google 계정 등이 있으면 삭제. 안 되면 공장초기화 |
| USB 디버깅 켜기 | 설정 → 태블릿 정보 → 빌드번호 7회 연속 탭 → 개발자 옵션 → USB 디버깅 켜기 |
| 와이파이 | 관리자 PC와 같은 와이파이에 연결 |
| 충전기 연결 | 세팅 중 방전 방지 |

계정이 남아 있으면 키오스크 잠금(Device Owner) 지정이 실패합니다.
안드로이드 규칙이라 우회할 수 없습니다.

## 4-2. 세팅 실행

1. 태블릿을 데이터 전송용 USB 케이블로 PC에 연결 (충전 전용 케이블 불가)
2. 태블릿에 "USB 디버깅을 허용하시겠습니까?" 가 뜨면 [항상 허용] 체크 후 [허용]
3. `태블릿-세팅.bat` 더블클릭

입력할 것은 두 가지입니다.

**도서관 주소**

```
  도서관 서브도메인 (전체 URL 붙여넣기도 가능, 비우면 앱 기본값 유지):
```

해당 도서관의 서브도메인만 입력합니다. 예: `splib` → https://splib.dobedub.com/home

**관리 서버 주소 — 중요**

```
  이 PC 의 넷버드 주소를 찾았습니다: http://100.x.x.x:8090
  이 주소로 설정할까요? (엔터=예 / 다른 주소를 직접 입력)
```

엔터를 누르면 됩니다. 자동으로 찾지 못한 경우 2-6에서 메모한 주소를
`http://100.x.x.x:8090` 형식 그대로 입력합니다.

이 단계를 건너뛰면 태블릿이 개발용 서버로 접속하도록 남아, 회사 대시보드에
나타나지 않습니다.

나머지(WebView 업데이트 → 앱 설치 → 잠금 → 세로 고정 → 넷버드 등록 →
기본 앱 정리 → 영상 투입 → 검증)는 자동입니다. 앱은 패키지 폴더의
`dobedub-kiosk-v*.apk` 를 자동으로 찾아 설치하고, WebView 단계는 파일이 동봉되지
않아 "건너뜁니다" 로 지나가는 것이 정상입니다. 중간에 태블릿에
"VPN 연결을 허용하시겠습니까?" 가 뜨면 [확인]을 누릅니다.

**완료 확인** — 아래 세 줄이 보이면 성공입니다. 케이블을 뽑아도 됩니다.

```
  [OK] Device Owner 지정 완료
  [OK] NetBird 등록 완료 — 태블릿 넷버드 IP: 100.x.x.x
  [OK] 관리 서버 주소를 기기에 설정: http://100.x.x.x:8090
```

## 4-3. 세팅 확인

1. 대시보드 기기 현황 탭에 태블릿이 나타나는지 확인 (최대 10분.
   태블릿 관리자 메뉴에서 "업데이트 확인"을 누르면 즉시 접속)
2. 기기 행에 "VPN 미지정" 경고가 없어야 정상

## 4-4. 도서관 설치 시

- 상시 충전 거치를 권장합니다. 화면이 켜져 있는 동안 배터리 소모가 큽니다.
- 도서관 와이파이에 연결만 하면 됩니다. 도서관 측 설정 요청은 필요 없습니다.

---

# 5부. 평소 운영

화면별 상세 조작은 `PC관리자_사용법.md` 참고.

## 5-1. 기본 원칙 두 가지

**지시는 태블릿이 접속할 때 실행됩니다.**

```
관리자에서 누름 → 대기 표시 → 태블릿 접속 → 실행 → 완료 표시
                              (몇 초 ~ 최대 10분)
```

**"미접속"은 고장이 아닙니다.** 화면이 꺼진 태블릿은 절전으로 접속이 늦어지고,
밤에는 아침까지 접속하지 않습니다.

| 표시 | 의미 | 조치 |
|---|---|---|
| 접속 중 | 정상 | — |
| 미접속 | 절전 중(정상) | — |
| 연락 두절 | 24시간 이상 무소식 | 이때만 확인 |

## 5-2. 자주 하는 작업

| 작업 | 방법 |
|---|---|
| 영상 등록 | `server\data\videos` 폴더에 mp4 복사 → 영상 자료실 탭에 자동 등록 |
| 태블릿에 영상 전송 | 기기 현황 → 해당 기기 [영상] → 체크 → 보내기 |
| 영상 표지 변경 | 영상 자료실 → 썸네일 등록/교체 |
| 홈 안내 이미지 | 기기 현황 → [이미지] |
| 앱 새 버전 배포 | 앱 배포 탭 → APK 선택 + 버전 입력 → 업로드 (이후 자동 설치) |
| 원격 재부팅 | 기기 현황 → [설정] → 원격 재부팅 |
| 문의 전화번호 변경 | 기기 현황 → [설정] → 문의 연락처 |
| PIN 분실 | 기기 현황 → [설정] → PIN 초기화 (0000으로 복구) |

## 5-3. 태블릿 관리자 화면

홈 화면 제목 "보이스툰 도서관"을 5회 연속 탭 → PIN `0000`.
키오스크 해제/재진입, Wi-Fi 설정, 업데이트 확인, 재부팅, 서버 주소 변경이 있습니다.

## 5-4. 백업 (월 1회 권장)

`server\data` 폴더를 외장하드나 클라우드에 통째로 복사합니다.
기기 목록·영상·안내 이미지가 모두 이 폴더에 있습니다.

---

# 6부. 문제 해결

| 증상 | 확인·조치 |
|---|---|
| 대시보드가 안 열림 | 서버 꺼짐 → `관리자-실행.bat` 실행 |
| 전부 빨간색(연락 두절) | PC 재부팅으로 서버가 꺼진 것 → `관리자-실행.bat` |
| 태블릿이 목록에 없음 | ① 4-2의 관리 서버 주소 입력 여부 ② 태블릿 관리자 → 원격 관리 → 서버 자동 찾기 ③ 넷버드 콘솔 Peers에 태블릿이 있는지 |
| 예전에 설치한 태블릿이 안 보임 | 정상. 0부 "이미 나가 있는 태블릿" 참고 — 회수 후 4부 재실행 |
| 다음 날 갑자기 전부 끊김 | 관리 PC의 넷버드 로그인 만료. 2-4의 Login Expiration 끄기 확인 |
| 새 태블릿 등록이 안 됨 | Setup Key 만료 → 새 키 생성(2-3). 기존 태블릿은 영향 없음 |
| 영상을 보냈는데 안 감 | 태블릿 접속 대기 중. 태블릿에서 "업데이트 확인"을 누르면 즉시 |
| 업데이트가 안 깔림 | 사용 중이면 미룸. 급하면 알림 보내기 또는 즉시 |
| 화면이 검게 멈춤 | [설정] → 원격 재부팅. 접속이 끊긴 상태면 현장에서 전원 재시작 |
| 소리가 안 남 / 더빙 이상 | 앱 버전 2.5.3 이상인지 확인 (이전 버전의 알려진 문제) |
| Device Owner 실패 | 태블릿에 계정 잔존 → 전부 삭제 후 재시도, 안 되면 공장초기화 |
| adb.exe 를 찾을 수 없음 | 1-2의 platform-tools 폴더 위치 확인 |
| 설치할 APK 를 찾을 수 없음 | 패키지 폴더의 `dobedub-kiosk-v*.apk` 가 `태블릿-세팅.bat` 과 같은 폴더에 있는지 확인. 지워졌으면 패키지 zip에서 다시 꺼내기 |
| "WebView APK 가 동봉되지 않아 건너뜁니다" | 정상. 패키지에 넣지 않는 파일이며 세팅은 계속 진행됩니다 |
| 넷버드 등록 건너뜀 | netbird-*.apk / ADBKeyboard.apk / netbird-setup-key.txt 중 누락 확인 |

## 넷버드 점검 순서

1. 회사 PC의 넷버드 앱이 Connected 인지 (아니면 Connect)
2. https://app.netbird.io/peers 에 PC와 태블릿이 모두 보이는지
3. 그룹: PC = `fleet-server`, 태블릿 = `kiosk`
4. PC의 Login Expiration이 꺼져 있는지

## 문의 시 함께 보낼 것

1. 대시보드 기기 현황 화면 캡처
2. 해당 태블릿의 기관명과 마지막 접속 시각
3. 무엇을 눌렀고 어떤 화면이 나왔는지

---

# 부록 A. 폴더 구성

| 파일/폴더 | 용도 |
|---|---|
| `서버-준비.bat` | 최초 1회, 서버 구성요소 설치 |
| `관리자-실행.bat` | 관리 서버 실행 |
| `관리자-종료.bat` | 관리 서버 종료 |
| `태블릿-세팅.bat` | 태블릿 세팅 |
| `dobedub-kiosk-v2.5.6.apk` | 태블릿 앱. `태블릿-세팅.bat` 이 이 폴더에서 이 이름으로 찾으므로 옮기거나 이름을 바꾸지 않습니다 |
| `ADBKeyboard.apk` | 세팅 중 자동 입력용 |
| `server\` | 관리 서버 (자료는 `server\data` 에 저장) |
| `준비물-여기에-넣으세요.txt` | 1부 준비물 안내 |
| `회사_인수인계_가이드.md` | 이 문서 |
| `PC관리자_사용법.md` | 대시보드 상세 사용법 |
| `납품_매뉴얼.md` | 태블릿 세팅 상세 절차 |
| `원격관리_NetBird_도입.md` | 넷버드 구조 (기술 문서) |
| `원격관리_업데이트.md` | 자동 업데이트 원리, 서명 키 경고 (기술 문서) |
| `README.md` | 앱 개요 (기술 문서) |
| `setup-tablet.ps1` 등 `.ps1` | 위 .bat 이 내부적으로 실행하는 파일. 직접 실행하지 않음 |
| `app\` | 대시보드의 기본 안내 이미지 미리보기용 리소스 |

`.md` 파일은 메모장으로도 열리지만 웹브라우저나 VS Code로 열면 표가 제대로 보입니다.

# 부록 B. 용어

| 용어 | 설명 |
|---|---|
| 넷버드(NetBird) | 회사 PC와 태블릿을 가상 사내망으로 묶는 프로그램 |
| Peer | 그 망에 등록된 기기 하나 |
| Setup Key | 태블릿을 로그인 없이 자동 등록시키는 키 문자열 |
| 그룹 | 기기 묶음. `kiosk`=태블릿, `fleet-server`=관리 PC |
| adb | PC가 USB로 안드로이드 기기를 제어하는 도구 |
| APK | 안드로이드 앱 설치 파일 |
| Device Owner | 기기 관리 권한을 가진 앱. 키오스크 잠금의 근거 |
| 체크인 | 태블릿이 서버에 접속해 상태를 보고하고 지시를 받는 것 |
| 대시보드 | 관리 화면 (http://localhost:8090/dashboard) |
| versionCode | 앱 버전 번호. 이 숫자가 커야 태블릿이 업데이트를 받음 |

# 부록 C. 개발 담당자 참고

- 현재 버전 v2.5.6 / versionCode 33. 새 APK의 versionCode는 반드시 이전보다
  커야 업데이트가 배포됩니다.
- 서명 키(`release-keystore.jks`)를 분실하면 배포된 태블릿을 다시는 업데이트할 수
  없습니다(기기별 공장초기화 필요). 백업 위치를 확인해 두세요.
- 개발용 서버(맥)와 이 운영 PC는 별개 함대입니다. 태블릿은 세팅 때 입력한 주소로만
  접속하므로 섞이지 않습니다.
- 넷버드 무료 플랜: 사용자 5명 / 기기 100대. 사용량은 콘솔 왼쪽 아래에 표시됩니다.
- 접속 규칙은 `kiosk → fleet-server:8090` 하나만 열려 있습니다. 기기가 늘어도
  그대로 두면 됩니다.
