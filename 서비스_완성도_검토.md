# 서비스 완성도 검토 — 도서관 아동용 보이스툰 키오스크

2026-08-22, 검토만(개발 없음). 대상: `mac-dev`(v2.5.3/code 30 + 서버) 및 납품선.
방법: 전 문서 통독 + 의심 지점은 코드로 사실 확인(파일:라인 근거 병기) + 이번 세션까지의 실측 이력.

## 0. 총평

**원격 함대 관리는 동종 규모 대비 과할 만큼 잘 갖춰졌다** — 즉시 지시(1~3초), 전송 진행률
양방향, 보고 기반 완료 판정, 원격 재부팅/주소 변경, always-on VPN 상태 경고, 자동 새로고침,
서버 테스트 16개. 반면 **"아이 앞에 놓인 기기"로서의 마감**과 **"몇 달을 사람 없이 버티는
서비스"로서의 운영 장치**에 빈 곳이 남아 있다. 아래는 그 목록이다.

---

## 1. 결함 후보 — 코드로 확인함 (P0: 납품 전 결정 필요)

### 1-1. 무조작 5분이면 **영상을 보는 중에도** 홈으로 튕긴다

- `MainActivity.kt:41` 기본 무조작 시간 5분. 모든 **터치**가 타이머를 리셋하지만
  (`dispatchTouchEvent`, :217), **재생 진행은 리셋하지 않는다.**
- `returnToHomeIfIdle()`(:251)은 "현재 화면이 홈인가"만 보고 **재생 중인지 확인하지 않는다.**
- 플레이어(`VideoPlayerScreen.kt`)의 `onUserInteraction` 호출은 전부 버튼 탭 핸들러 안이다 —
  재생 루프(0.5초 위치 갱신, :103)는 부르지 않는다.

→ **5분보다 긴 영상을 얌전히 보던 아이는 재생 도중 홈 화면으로 쫓겨난다.** 웹툰 감상·더빙
재생(웹뷰)도 자동 진행 중 터치가 없으면 동일하다. 현장 보고가 없었다면 ① 운영자가 무조작
시간을 길게 설정했거나 ② 아이들이 계속 화면을 만지기 때문일 가능성이 크다 — 어느 쪽이든
설계가 지켜주는 게 아니라 우연이 지켜주는 상태다.
**제안(추후 개발)**: 영상 재생 중 + 웹뷰 오디오 재생 중에는 idle 판정을 유예. 한 줄 요약:
"무조작"의 정의에 "감상 중"을 포함시킬 것.

### 1-2. 웹뷰 오류 화면은 있으나 복구 동선이 약하다

`RestrictedWebViewScreen.kt:432` `onReceivedError` → "인터넷 연결을 확인해주세요" 표시까지는
있다(양호). 다만 아이가 할 수 있는 다음 행동(다시 시도 버튼, 문의 연락처 노출 여부)이
화면에 있는지 명세가 없다 — 도서관 와이파이가 잠깐 끊겼다 돌아오는 흔한 상황에서
**아이 혼자 복구 가능한가**를 기준으로 한 번 실기기 점검 권장(검토 범위에서는 코드 존재만 확인).

---

## 2. 아동 이용 관점 (P1)

| 항목 | 현재 | 갭 |
|---|---|---|
| 청력 보호 | 볼륨 상한 설정 존재(관리자 기기 설정, 볼륨 키 가로채기 `MainActivity.kt:223`) | **권장값 지침이 없다.** 납품 매뉴얼에 "아동 기준 최대 몇 %"를 명시할 것(WHO 아동 청력 가이드 기준 85dB 상당). 스피커 실측 1회면 정할 수 있다 |
| 위생 | — | 공용 터치스크린 + 내장 마이크에 아이들이 입을 대고 더빙한다. **소독 주기/방법 운영 지침이 어디에도 없다.** 납품 매뉴얼 한 절이면 된다(개발 불필요) |
| 파손·오조작 | Lock Task + PIN, 문의 연락처 표시 | 충분 |
| 접근성 | 미검토 | 저시력/청각 보조는 사이트 영역이지만, 키오스크 거치 높이(휠체어)는 납품 안내에 한 줄 넣을 만함 |
| 콘텐츠 안전 | 도메인 화이트리스트 코드 강제 | 충분 — 외부 이탈 불가 확인됨 |

## 3. 신뢰성·관제 (P1 — 대수 늘기 전에)

1. **능동 알림이 없다.** 연락 두절·VPN 미지정·업데이트 실패 전부 **대시보드를 열어야만**
   보인다. 기기 3대인 지금은 되지만, 도서관 수십 대가 되면 "어제부터 죽어 있었는데 아무도
   몰랐다"가 반드시 생긴다. 서버가 임계 상태에서 메일/카톡 알림 한 번 쏘는 것이 다음 단계.
2. **앱이 죽으면 관제도 같이 죽는다**(지시 채널이 앱 안 — CLAUDE.md 문서화됨). 원격 재부팅도
   앱이 살아 있어야 닿는다. 별도 watchdog 없음. Lock Task 홈 앱이라 크래시 시 시스템이
   재기동해주는 것에 기대는 구조 — "검은 화면" 간헐 증상이 이 기대의 구멍이다.
3. **원격 진단 수단이 체크인 보고뿐이다.** 원격 스크린샷/로그 수집이 없어, 이번 `<queries>`
   사고처럼 **케이블을 꽂기 전까지 원인 판별이 불가능**한 부류가 남아 있다. (당시 재부팅
   QA 한 사이클을 날린 원인이 정확히 이것.)
4. **관리자 서버가 PC 재부팅 시 자동으로 안 뜬다** — 문서에 "다시 실행하라"고만 적혀 있다.
   윈도우 작업 스케줄러/시작프로그램 등록이면 끝나는 일(개발 불필요, 납품 매뉴얼 절차 추가).
5. **WebView 가 영구 고정이다**(스토어 없는 기기). 버그 우회(dvh 등)가 계속 유효한 장점과
   보안 패치 부재라는 단점이 공존 — 연 1회 세팅 스크립트로 WebView APK 수동 갱신을 운영
   주기에 넣을지 결정 필요.
6. doze 야간 두절(14~16시간, 실측 문서화)은 알려진 트레이드오프 — 야간에 원격 지시할 일이
   생기면 `AlarmManager` 전환(문서에 검토 항목으로 존재).

## 4. 이용 통계 부재 (P1 — 사업 관점에서는 P0일 수 있음)

**키오스크에서 무엇이 얼마나 쓰이는지 아무 데도 남지 않는다.** 로컬 영상 재생 횟수·시간,
버튼별 진입 횟수는 사이트 애널리틱스로도 잡히지 않는다(웹뷰 밖). 도서관에 "이번 분기
이용 실적"을 낼 수 없다는 뜻 — **재계약·추가 납품의 근거 자료**가 지금 구조에서는 없다.
체크인 payload 에 일별 카운터 몇 개 싣는 정도의 작은 개발로 시작 가능(개인 식별 없이 집계만 —
아동 서비스라 이 원칙이 중요).

## 5. 보안·개인정보 (P1~P2)

| 항목 | 상태 | 판단 |
|---|---|---|
| 아동 음성 | 키오스크는 저장 안 함(실시간 체인만) — 녹음 데이터는 사이트가 처리 | **만 14세 미만 음성 = 개인정보. 동의 체계(법정대리인)가 사이트/서비스 약관에 있는지 회사 차원 확인 필요.** 키오스크 문제는 아니나 납품 계약서에 걸릴 수 있는 항목 |
| 평문 HTTP 전면 허용 | v1.6 이후, 문서화됨 | NetBird 구간은 WireGuard 로 암호화되므로 실질 노출은 LAN 구간뿐. 공인 주소 전환 시 재검토(기존 결론 유지) |
| 대시보드 인증 | 공유 비번 1개, 로그인 시도 제한 없음(`auth.js` 확인), `SESSION_SECRET` 미설정 경고 방치 | 노출면이 LAN+VPN(ACL: kiosk→8090만)이라 낮음. 서버를 공인망에 내놓는 순간 P0 로 승격 — 그 전까지는 `SESSION_SECRET` 만 설정해 재시작 로그아웃이나 없앨 것 |
| 기기 인증 | `DEVICE_TOKEN` 옵션이 앱·서버 양쪽에 구현돼 있으나 **미사용** | 지금 위협 모델에서는 선택. 공인망 전환 시 필수 |
| 공개 리포 | 비밀 전부 gitignore(반복 검증됨) | 유지. 새 비밀 추가 시 같은 규율 |

## 6. 데이터 보존·백업 (P0 하나 포함)

1. **서명 키 백업의 실체 확인 — P0.** 문서는 "별도 백업 필수"라고만 한다. 지금 이 순간
   `release-keystore.jks` 가 **몇 곳에, 어디에** 존재하는가? 맥 1곳뿐이라면 이 검토에서
   가장 급한 항목이다(분실 = 전 기기 공장초기화, 문서에 명시된 그대로).
2. `server/data/`(DB·영상·이용안내) 백업 절차 없음 — 주 1회 복사 한 줄이면 됨.
3. `checkins` 무한 증가 — 문서화됨, 수십 대 전에 90일 정리 넣기.
4. NetBird 계정(devops@) 복구 수단 회사 관리 — 문서화됨, 실행 여부 확인 필요.

## 7. 납품·확장 잔여 (진행 중 항목 정리)

- QA 잔여: QA-3 망 전환, QA-5 밤샘 doze 정식 기록, QA-6 세팅 스크립트 5.8 신규 기기 첫 완주.
- 기존 3대: v2.5.2 미만 → 재부팅 시 넷버드 수동 복구 필요 상태. 마스터 PC 복귀 시
  일괄 업데이트 + `setFleetUrl` 이관(절차 문서화 완료).
- versionCode 충돌(27~29 두 계열): 규칙 확정됨(납품선 28~30, 개발선 31+) — 지킬 것만 남음.
- 마스터 PC 를 서버로 되돌릴 때: NetBird **setup key** 로 등록(세션 만료 없음), 앱 기본
  주소 교체 재배포 또는 setFleetUrl 일괄 변경.
- 수십 대 규모: APK 전송 부하 → 보고팡 AWS(S3/CloudFront) 이전 계획 문서화돼 있음.

## 8. 우선순위 요약

| 순위 | 항목 | 성격 |
|---|---|---|
| **P0** | 서명 키 백업 실체 확인(§6-1) | 확인만, 5분 |
| **P0** | 감상 중 무조작 홈 복귀(§1-1) — 방침 결정 | 결정 후 소규모 개발 |
| P1 | 위생·볼륨 권장값·서버 자동 시작 — 납품 매뉴얼 3절 추가 | 문서만 |
| P1 | 이용 통계 최소 세트(§4) | 소규모 개발 |
| P1 | 능동 알림(연락 두절/VPN 미지정)(§3-1) | 소규모 개발 |
| P1 | 아동 음성 동의 체계 확인(§5) | 회사 확인 |
| P2 | 원격 로그/스크린샷, watchdog, WebView 갱신 주기, SESSION_SECRET, checkins 정리 | 여유 시 |

> 이 문서는 검토 결과이며 어떤 항목도 아직 구현하지 않았다. P0 두 건은 개발 이전에
> **결정/확인**이 먼저다.
