# 태블릿 납품 세팅 매뉴얼

두비덥 도서관 키오스크를 **새 태블릿에 설치하는 담당자용** 안내입니다.
개발/빌드 환경은 [DEV_SETUP.md](DEV_SETUP.md), 기술 배경은 [기획문서.md](기획문서.md) 참고.

---

## 한눈에 보기

> **케이블 연결 → 태블릿에서 "USB 디버깅 허용" → `태블릿-세팅.bat` 더블클릭 → 끝**

나머지(WebView 업데이트, 앱 설치, 키오스크 잠금, 샘플 영상 넣기, 실행 검증)는 스크립트가 자동으로 합니다.

---

## ⚠️ 시작 전 꼭 알아야 할 것 — 계정

태블릿을 키오스크로 잠그려면 **"기기 소유자(Device Owner)"** 권한이 필요한데,
Android 보안 정책상 이 권한은 **기기에 계정이 하나도 없을 때만** 지정할 수 있습니다.

| 태블릿 상태 | 해야 할 일 |
|---|---|
| **새 제품(개봉 후 미설정)** | 초기 설정 마법사에서 **Wi-Fi와 Google 로그인을 "건너뛰기"** 로 진행 → 바로 세팅 가능 |
| **이미 쓰던/로그인된 기기** | **계정을 모두 삭제**(설정 > 계정) 하거나 **공장초기화** 후 초기설정에서 로그인 건너뛰기 |

세팅 스크립트가 계정을 자동으로 감지해서, 남아있으면 알려주고 멈춥니다. (계정은 보안상 PC에서 대신 지울 수 없어, 태블릿에서 직접 지워야 합니다.)

---

## 준비물 (PC 1회 세팅)

처음 쓰는 납품용 PC라면 아래를 한 번만 갖추면 됩니다.

1. **이 프로젝트 폴더** (`DobedubKiosk`) 와 그 **상위 폴더**에 있는:
   - `app\build\outputs\apk\debug\app-debug.apk` — 키오스크 앱 (없으면 `gradlew assembleDebug`로 빌드)
   - `..\com.google.android.webview_*.apk` — WebView 업데이트 파일
   - `..\[두비덥 보이스툰] ...\*.mp4` — 샘플 동영상
2. **adb (platform-tools)** — 아래 중 아무 곳에나 있으면 스크립트가 자동으로 찾습니다:
   - PC에 Android SDK가 깔려 있거나 (`%LOCALAPPDATA%\Android\Sdk\platform-tools`)
   - adb가 PATH에 있거나
   - **가장 간단**: [platform-tools](https://developer.android.com/tools/releases/platform-tools) 압축을 풀어 `platform-tools` 폴더를 **이 스크립트 옆에** 복사
3. **USB 드라이버** — 대부분 Windows가 자동 인식. 안 잡히면 제조사(Lenovo) USB 드라이버 설치.

---

## 태블릿 세팅 (1대마다 반복)

### 1) 태블릿에서 USB 디버깅 켜기 (기기당 1회)
- 설정 > **태블릿 정보** > **빌드번호**를 7번 연속 탭 → "개발자가 되었습니다"
- 설정 > 시스템 > **개발자 옵션** > **USB 디버깅** 켜기

### 2) 케이블로 PC와 연결
- **데이터 전송용** USB 케이블 사용 (충전 전용 케이블 ❌)
- 태블릿 화면에 뜨는 **"USB 디버깅을 허용하시겠습니까?"** → **[허용]**
  (재실행 편하게 "이 컴퓨터에서 항상 허용" 체크 권장)

### 3) `태블릿-세팅.bat` 더블클릭
검은 창이 뜨고 아래 순서로 자동 진행됩니다:

```
0. adb 준비
1. 태블릿 연결        ← 아직 케이블 안 꽂았으면 여기서 기다립니다
2. 사전 점검          ← 계정이 있으면 여기서 멈추고 안내
3. WebView 업데이트
4. 키오스크 앱 설치
5. Device Owner 지정  ← 키오스크 잠금 권한
6. 기본 앱 정리        ← 불필요한 선탑재 앱(크롬/유튜브/넷플릭스/오피스 등) 제거
7. 샘플 동영상 투입    ← 용량이 커서 수 분 걸릴 수 있음(이미 있으면 건너뜀)
8. 앱 실행 및 검증
```

> **기본 앱 정리(6단계)**: 키오스크에 불필요한 소비자 앱만 골라 제거합니다(`pm uninstall --user 0`).
> WebView·키보드·시스템 UI·Play 서비스·런처 등 **동작에 필요한 것은 절대 건드리지 않습니다.** 실기기(Lenovo TB-J606F)에서
> 33개 제거 후에도 웹툰 재생/키오스크 잠금 정상 동작을 확인했습니다. 제거된 앱은 **공장초기화로 모두 복구**됩니다.
> 제거 목록은 `setup-tablet.ps1` 상단 `$BloatPackages` 에서 조정할 수 있고, 정리를 아예 건너뛰려면 `-SkipDebloat` 옵션을 씁니다.

**"세팅 완료!"** 초록 박스가 뜨면 성공입니다.

### 4) 태블릿에서 최종 확인
1. 홈 화면 **로고를 5번 탭** → 기본 PIN **`0000`** 으로 관리자 진입
2. **PIN 변경**, 시작 URL / 허용 도메인 확인
3. **동영상 보기** — 넣은 영상이 보이는지
4. **도서관 웹사이트** — 웹툰 재생이 정상인지

---

## 자주 나는 문제

| 증상 | 원인 / 해결 |
|---|---|
| "5분 동안 태블릿을 찾지 못했습니다" | 충전 전용 케이블일 수 있음 → 데이터용으로 교체. USB 디버깅 켰는지 확인. |
| 창에 "USB 디버깅 허용을 눌러주세요" 반복 | 태블릿 화면의 허용 팝업을 눌러야 진행됨. 팝업이 안 뜨면 케이블 뺐다 다시 연결. |
| "계정이 N개 등록되어 있습니다" | 태블릿 설정 > 계정에서 **모두 삭제** 후 `.bat` 재실행. 안 지워지면 공장초기화. |
| "Device Owner 지정 실패" | 대개 계정이 남아있거나 이미 다른 프로필 존재 → 공장초기화 후 로그인 건너뛰고 재시도. |
| "기존 설치본과 서명이 다릅니다" | 다른 PC에서 설치했던 앱이 있음. 태블릿 관리자 메뉴 > **키오스크 완전 해제** 후 재실행. |
| WebView 업데이트 실패 | 이미 최신이면 무시하고 계속 진행됨. 웹툰이 안 뜨면 이 단계 재확인. |

---

## 참고

- **여러 대 연속 세팅**: 한 대 끝나면 케이블만 다음 태블릿에 꽂고 `.bat` 다시 더블클릭.
  이미 넣은 영상은 크기가 같으면 자동으로 건너뛰어 빠릅니다.
- **영상 다시 넣기**: 창을 닫고 `powershell -ExecutionPolicy Bypass -File setup-tablet.ps1 -ForceVideo`
- **영상/ WebView 없이 앱만**: `... -SkipVideo -SkipWebView`
- **반납/회수 시 잠금 해제**: 태블릿 관리자 메뉴 > 키오스크 완전 해제 (Device Owner 반환). 되돌릴 수 없으니 2단계 확인 후 진행.
