# Tailscale 원격 관리 기획 (개발 전 — 계획 문서)

작성 2026-08-12. **아직 아무것도 구현하지 않았다.** 이 문서는 도메인/터널 없이
Tailscale 로 "관리자 PC ↔ 전국 도서관 태블릿"을 상시 연결하는 계획이다.

## 0. 왜 Tailscale 인가 (결정 배경)

| 후보 | 탈락/보류 사유 |
|---|---|
| Cloudflare Quick Tunnel (현행) | **재시작마다 주소가 바뀜** — 외부 태블릿 전부 재설정 필요. 현행 운영 최대 마찰 |
| Cloudflare 명명 터널 | 주소 고정·무료·무제한이지만 **도메인 구입 + DNS 이관** 필요 (보류 중) |
| ngrok 무료 | 월 1GB — 영상 1개 배포로 소진 |
| localtunnel | 태블릿에서 HTTP 511. 고정 서브도메인 재시도도 connection refused (실측 2회 실패) |
| **Tailscale** | 무료(100대), 도메인 불필요, **주소 영구 고정**, WireGuard P2P라 대용량 영상에 유리, 서버가 인터넷에 노출되지 않음 |

트레이드오프: 태블릿마다 앱 하나(Tailscale)가 더 깔리고, 그 앱의 로그인·생존이
관리 대상이 된다. 이 문서의 절반은 그 관리를 자동화하는 내용이다.

> 약관(무료 플랜의 업무용 사용) 검토는 사용자 지시로 **보류**. 납품 전 재확인 항목으로만 남긴다.

## 1. 목표 아키텍처

```
[관리자 PC]                          [도서관 A 태블릿]
 fleet server :8090                   키오스크 앱 ──┐
 Tailscale (Windows 서비스)           Tailscale ←───┘ 체크인: http://100.x.y.z:8090
      │ 100.x.y.z (영구 고정)              │
      └────────── tailnet ────────────────┴── [도서관 B 태블릿] …
```

- 태블릿은 **어느 와이파이에 있든** `100.x.y.z:8090` 하나로 체크인한다. 주소는 영원히 안 바뀐다.
- 같은 사무실(LAN)에서는 기존 자동 찾기/축약 입력이 그대로 동작한다. Tailscale은 추가 경로일 뿐, 기존 경로를 대체하지 않는다.
- 서버 코드는 **수정 없음**: 이미 `0.0.0.0` 바인드라 tailnet IP로도 응답하고, 체크인 응답의 절대 URL(`apkUrl`, `pushVideos[].url`)은 요청 Host 기준으로 만들어져 tailnet 접속엔 tailnet 주소가 실린다.

## 2. 구성 요소별 계획

### 2.1 Tailscale 계정/tailnet

- 계정 1개로 시작 (무료: 사용자 3명 / 기기 100대). **회사 공용 구글 계정** 권장 —
  개인 계정에 물리면 퇴사/계정 분실이 곧 전 함대 장애다. ⚠ 어떤 계정을 쓸지 결정 필요.
- 태블릿은 전부 `tag:kiosk` 태그로 등록한다. 태그 기기는 **키 만료가 기본 비활성** —
  "반년 뒤 조용히 전부 이탈" 리스크가 원천 차단된다. (관리자 PC는 태그 없이 등록되므로
  admin console에서 key expiry 를 수동으로 꺼줘야 한다. ⚠ 체크리스트에 포함)
- 사전 인증 키: **reusable + tag:kiosk** 로 1개 발급해 세팅 스크립트에 넣는다.
  auth key 자체의 유효기간은 최대 90일 → 만료되면 새 키 발급해서 스크립트만 갱신
  (이미 등록된 기기는 영향 없음, 신규 세팅에만 필요).

### 2.2 ACL (필수 — 기본값은 전부 열림)

기본 ACL은 tailnet 내 모든 기기가 서로 전 포트로 통신 가능하다. 즉 **태블릿끼리도,
태블릿→관리자 PC의 8090 외 포트도** 다 열린다. 관리자 PC에는 다른 서비스(웍스메일
자동화 등)도 돌므로 반드시 좁힌다:

```json
{
  "tagOwners": { "tag:kiosk": ["autogroup:admin"] },
  "acls": [
    // 태블릿 → 관리자 PC의 함대 서버 포트만
    { "action": "accept", "src": ["tag:kiosk"], "dst": ["100.x.y.z:8090"] },
    // 관리자(사람) → 전부 (디버깅용)
    { "action": "accept", "src": ["autogroup:admin"], "dst": ["*:*"] }
  ]
}
```

태블릿으로 들어오는 인바운드는 아무도 필요 없다 → 자동으로 차단됨.

### 2.3 관리자 PC

| 항목 | 계획 |
|---|---|
| Tailscale 설치 | Windows 클라이언트. 서비스로 돌므로 **부팅 시 자동 연결** (로그인 불필요) |
| 함대 서버 자동 시작 | ⚠ **현재 빠져 있는 조각** (실측: 스케줄러/시작프로그램/서비스 어디에도 없음). 작업 스케줄러 `onstart` 등록. Tailscale과 무관하게 지금도 필요한 작업 |
| 절전 | 전원 옵션에서 절전/최대절전 해제 (24시간 PC 전제) |
| Quick Tunnel | Tailscale 검증 완료 후 폐기 (`start-tunnel.ps1` 은 문서용으로 남김) |

### 2.4 태블릿 (세팅 스크립트 확장)

`setup-tablet.ps1` 에 **4.5단계 "Tailscale 설치·가입"** 추가:

```
4.  키오스크 APK 설치            (기존)
4.5 Tailscale APK 설치
    → adb install tailscale.apk        (APK는 스크립트 옆에 동봉)
    → 사전 인증 키로 무인 로그인        (방법은 아래 A/B 중 검증 후 택1)
    → Device Owner로 always-on VPN 지정
5.  Device Owner 지정            (기존)   ← 순서 주의: always-on VPN 지정은
                                            Device Owner 지정 *후*에만 가능하므로
                                            실제로는 5단계 뒤에 5.7로 넣는다
```

무인 로그인 후보 (⚠ 실기기 검증으로 확정):

- **A. 관리 구성(App Restrictions)** — 권장. Device Owner가
  `setApplicationRestrictions("com.tailscale.ipn", {authKey: "tskey-…"})` 를 밀어넣으면
  Tailscale이 첫 실행 때 읽어 자동 가입. 화면 조작 자체가 없어 스크립트화가 가장 깔끔.
  단, 이 방식은 우리 앱(Device Owner)에 "관리 구성을 미는" 소량의 코드가 필요할 수도,
  `adb shell dpm`/`am` 조합으로 될 수도 있다 — 검증 항목.
- **B. 인텐트 방식** — `am start -n com.tailscale.ipn/… -e authkey tskey-…`.
  Tailscale 버전에 따라 액티비티명이 달라진 이력이 있어 동봉할 APK 버전에 맞춰 확인.

always-on VPN:

```kotlin
dpm.setAlwaysOnVpnPackage(admin, "com.tailscale.ipn", /*lockdown=*/false)
```

- **lockdown=false 로 한다.** true면 VPN이 죽는 순간 태블릿의 모든 통신이 끊긴다 —
  도서관 웹뷰(공인 HTTPS 직결)까지 죽으므로 키오스크에는 과하다. false면 Tailscale이
  죽어도 서비스는 살고 원격 관리만 잠시 끊긴다. 실패 모드가 훨씬 온화하다.
- 재부팅 후 자동 연결이 이 설정의 핵심 목적. BootReceiver에서 추가로 챙길 게 있는지 검증.

### 2.5 키오스크 앱 (개발 시점의 변경 — 최소)

| 변경 | 내용 | 필수? |
|---|---|---|
| 없음 | `100.x.y.z` 입력 시 `normalizeFleetUrl` 이 이미 `http://100.x.y.z:8090` 으로 펼침 | — |
| 기본 서버 주소 교체 | `gradle.properties` 의 `fleetServerUrl` 을 tailnet 주소로 → **신규 태블릿은 서버 주소 입력 자체가 불필요**해짐 (현 기본값 `https://kiosk.dobedub.com` 은 죽은 주소) | 권장 |
| always-on VPN 지정 코드 | 세팅 스크립트에서 dpm 커맨드로 될지, 앱 코드가 필요할지에 따라 | 검증 후 |

## 3. 영상 배포(대용량) 경로 계획

- 같은 건물/공유기: 기존 LAN 직결 그대로 (Tailscale 개입 없음).
- 외부 도서관: Tailscale P2P 직결이 되면 회선 속도 그대로. **P2P가 막히면(도서관
  방화벽이 UDP 차단) DERP 릴레이로 우회**되는데 이때 수 Mbps 수준까지 떨어질 수 있다.
  1GB 영상이면 체감 큼.
- 대응: 테스트 단계에서 `tailscale ping <기기>` 와 `tailscale status` 로 각 도서관이
  direct 인지 relay 인지 확인해 기록한다. relay 도서관이 많으면 그때 "영상만 S3/외부
  스토리지" 같은 우회를 검토 (지금은 안 만든다 — YAGNI).

## 4. 실패 모드와 대비

| 시나리오 | 영향 | 대비 |
|---|---|---|
| 도서관 와이파이가 캡티브 포털 | **Tailscale로도 못 넘음** — 인터넷 자체가 안 됨. 키오스크는 잠겨 있어 동의 페이지를 누를 수 없음 | 납품 전 도서관별 와이파이 방식 확인 절차를 납품 매뉴얼에 추가. 포털 방식이면 MAC 등록/예외 요청 |
| UDP 차단 → DERP 릴레이 | 체크인은 문제없음(소량), 영상 배포만 느림 | §3. 도서관별 direct/relay 기록 |
| Tailscale 앱 죽음 | lockdown=false라 서비스는 정상, 원격 관리만 끊김 | always-on VPN이 재기동. 대시보드 오프라인 표시로 감지 |
| 관리자 PC 재부팅 | Tailscale은 서비스라 자동 복귀. **함대 서버는 현재 수동** | 작업 스케줄러 등록 (§2.3) |
| auth key 90일 만료 | 신규 세팅만 실패, 기존 기기 무관 | 키 재발급 절차를 납품 매뉴얼에 기록 |
| 계정 접근 상실 | 신규 등록 불가 (기존 기기는 동작 지속) | 회사 공용 계정 + 복구 수단 문서화 |
| PC 교체/이사 | tailnet IP가 **기기에 귀속**이라 새 PC는 새 IP | 서버 이전 시 전 기기 주소 재설정 필요. 이를 피하려면 그때 MagicDNS 이름(`pc이름.tailnet.ts.net`)으로 전환 검토 |

## 5. 검증 계획 (연결된 HA1EHJC2 1대로, 개발 착수 전 확인)

1. **수동 스모크 테스트** (스크립트화 전에 손으로):
   PC와 태블릿에 Tailscale 수동 설치·로그인 → 태블릿 관리자 화면에 `100.x.y.z` 입력
   → 체크인 성공 확인. *여기까지는 코드 제로.*
2. **다른 망 시뮬레이션**: 태블릿을 휴대폰 핫스팟에 물리고(사무실 LAN 이탈) 체크인
   유지 + 관리자에서 "업데이트 확인" 동작 확인. direct/relay 여부 기록.
3. **재부팅 생존**: 태블릿 재부팅 → 부팅 완료 후 자동 체크인 오는지 (always-on VPN 검증).
4. **Lock Task 공존**: 키오스크 잠금 상태에서 VPN 유지되는지, VPN 동의 다이얼로그가
   잠금 화면을 뚫고 떠서 걸리적거리지 않는지.
5. **대용량**: 핫스팟 상태에서 1GB 영상 푸시 → 소요 시간, direct/relay 기록.
6. **무인 가입**: §2.4 A/B 중 어느 쪽이 이 Tailscale APK 버전에서 동작하는지 확정.
7. 전부 통과하면 → setup-tablet.ps1 확장 + gradle 기본 주소 교체 개발 착수.

각 항목은 통과/실패와 측정값을 이 문서에 추기한다.

## 6. 하지 않기로 한 것 (명시적 제외)

- **Tailscale SSH / 원격 셸**: 함대 서버 체크인 프로토콜로 충분. 필요해지면 그때.
- **subnet router / exit node**: 태블릿이 PC의 8090 하나만 보면 되므로 불필요.
- **Headscale(자체 호스팅)**: 관리 대상만 늘어남. 무료 한도(100대)가 문제 될 규모가 아님.
- **MagicDNS 즉시 도입**: IP 하나로 시작. PC 교체 시점에만 재검토 (§4 마지막 행).

## 7. 사용자 결정 필요 (개발 착수 전)

1. **Tailscale 계정**: 어떤 계정으로 만들지 (회사 공용 권장).
2. **ACL 강도**: §2.2 제안(태블릿→8090만)으로 좁힐지, 초기엔 기본(전부 열림)으로 두고 검증 후 좁힐지.
3. **기기 이름 규칙**: admin console 식별용. 제안: `kiosk-<기관라벨>-<순번>` (예: kiosk-jbegec-01).
4. (보류 중) 무료 플랜 약관 — 납품 결정 시점에 재확인.
