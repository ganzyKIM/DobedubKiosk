# PC 관리자로 태블릿 다루기

관리자 PC에서 도서관 태블릿을 원격으로 관리하는 방법. **v2.0.3 / 3탭 대시보드 기준.**

원리·구조가 궁금하면 `원격관리_업데이트.md`(자동 업데이트 동작, 서명 키),
`server/README.md`(API·서버 운영)를 볼 것. 이 문서는 "화면에서 뭘 눌러야 하나"만 다룬다.

---

## 0. 시작하기

**`관리자-실행.bat` 더블클릭.** 서버가 뜨고 브라우저가 자동으로 열린다.

```
  ->    주소: http://localhost:8090/dashboard
  ->    비밀번호: dobedub
  ->    태블릿용 주소: http://172.31.0.162:8090  (관리자 화면에 입력)
```

- 이미 켜져 있으면 다시 띄우지 않고 브라우저만 연다. 여러 번 눌러도 안전하다.
- 서버는 창을 닫아도 계속 돈다. 끄려면 `관리자-종료.bat`.
- **PC를 재부팅하면 자동으로 뜨지 않는다.** 재부팅 후 `관리자-실행.bat` 을 한 번 눌러야
  태블릿들이 다시 붙는다.
- "태블릿용 주소"는 공유기 상황에 따라 바뀔 수 있어서 실행할 때마다 다시 찍어준다.

화면은 탭 3개다.

| 탭 | 언제 쓰나 |
|---|---|
| **기기 현황** | 매일 보는 화면. 어느 태블릿이 살아 있나, 뭘 갖고 있나 |
| **앱 배포** | 새 앱 버전을 내보낼 때 |
| **영상 자료실** | 영상을 등록·정리할 때 (보내는 건 기기 현황 탭에서) |

---

## 1. 가장 먼저 알아야 할 두 가지

이걸 모르면 "고장 났나?" 하고 헤매게 된다.

### ① 모든 지시는 즉시 실행되지 않는다

관리자에서 누르는 것(영상 보내기, 삭제, PIN 초기화, 연락처 변경, 업데이트 알림)은
전부 **대기열에 쌓였다가 태블릿이 다음에 접속할 때** 실행된다.

```
관리자에서 누름  →  대기 상태로 표시  →  태블릿이 접속  →  실제 실행  →  완료 표시
                                        (최대 30분, 절전 중이면 더)
```

**즉시 반영하고 싶으면** 태블릿에서 관리자 메뉴 → **업데이트 확인** 을 누르면 된다.
그 버튼이 즉시 접속을 일으킨다.

### ② "미접속"은 고장이 아니다

태블릿은 **사용 중일 때 30분마다** 접속한다. 화면이 꺼져 있으면 안드로이드 절전 때문에
1~2시간까지 늦어지고, **밤에는 아침까지 끊긴다**(실측 14~16시간). 정상이다.

| 표시 | 뜻 |
|---|---|
| 🟢 접속 중 | 70분 안에 접속함 |
| 🟡 미접속 | 그보다 오래됨 — 대개 화면 꺼짐/절전 |
| 🔴 연락 두절 | **24시간 넘게 소식 없음** — 이때만 실제로 확인이 필요 |

밤에 노란색이 뜨는 건 정상이고, **빨간색이 며칠 가면** 전원/와이파이/기기 고장을 의심한다.

---

## 2. 기기 현황 탭

### 표 읽는 법

| 열 | 내용 |
|---|---|
| 마지막 접속 | "12분 전" + 상태 + 누적 접속 횟수 |
| 기기 | **기관명(수정 가능)** / 모델 · 접속 중인 와이파이 이름 · 지도 / 기기 ID |
| 버전 | 앱 버전 + `최신` 또는 `업데이트 필요` + 알림 버튼 |
| 전원 | 🔒 키오스크 잠김 / 🔓 해제됨, 배터리 % |
| 영상 | 보내기 버튼 + 보유 목록 (펼치기) |
| 관리 | 연락처 · PIN 초기화 · 기록 삭제 (펼치기) |

### 기관명 붙이기

기기 ID(`fa4c6d9de8e2e8a7`)만으론 어느 도서관인지 모른다. **기관명 칸에 입력하고 엔터**
하면 바로 저장된다. 납품 시 세팅 스크립트에서 넣은 값이 기본으로 들어가 있다.

기기를 구분하는 다른 단서는 **접속 중인 와이파이 이름(📶)** 이다. 도서관마다 AP가 다르니
같은 기관에 여러 대가 있어도 어디 있는지 짐작할 수 있다.
지도(📍)는 나오는 기기에서만 뜬다 — 대부분의 납품 태블릿은 좌표가 안 잡힌다(정상).

### 태블릿이 목록에 아예 없을 때

그 태블릿에 **서버 주소가 설정되지 않은 것**이다. 태블릿에서:

1. 홈 화면 제목 **"보이스툰 도서관"을 5회 탭** → PIN `0000`
2. **원격 관리** → 서버 주소 입력
   - 같은 와이파이면 **"이 와이파이에서 서버 자동 찾기"** 버튼이 제일 빠르다
   - 직접 칠 땐 `관리자-실행.bat` 이 알려준 "태블릿용 주소"를 넣는다
   - 같은 공유기면 **마지막 자리만** 쳐도 된다 (예: `162` → `http://172.31.0.162:8090`)
3. 저장 후 관리자 메뉴에서 **업데이트 확인** 을 누르면 즉시 접속한다

---

## 3. 앱 업데이트 (앱 배포 탭)

### 새 버전 내보내기

1. **앱 배포** 탭 → "새 버전 배포"
2. APK 파일 선택, `versionCode` 와 `versionName` 입력
   - **versionCode 는 반드시 기존보다 커야 한다.** 이 숫자가 커야만 기기가 업데이트한다
   - 릴리스 메모는 **이 폼에서 입력해야 한글이 안 깨진다** (명령줄 curl 로 올리면 깨짐)
3. 업로드하면 즉시 "현재 배포 버전"이 된다

그 다음은 **자동**이다. 각 태블릿이 접속할 때 새 버전을 발견하면, **홈 화면에서 아무도
안 쓰고 있을 때 조용히 설치**한다. 영상·웹툰을 보는 중엔 방해하지 않고 다음 기회로 미룬다.

### 급할 때 — 알림 보내기

기다리지 않고 밀어붙이려면 **"업데이트 필요 기기(N대)에 알림 보내기"**.
그러면 태블릿 홈 화면에 확인창이 뜨고, **이용자가 "지금 업데이트"를 눌러야** 설치된다.
누르기 전까지는 `🔔 알림 대기중` 으로 표시된다. 개별 기기만 보내려면 기기 표의
버전 칸에 있는 **알림 보내기** 버튼을 쓴다.

### 되돌리기 (롤백)

"배포 이력"에서 원하는 버전의 **"이 버전으로 롤백"**. 올려둔 APK 를 다시 배포판으로
지정하는 것이라 재업로드가 필요 없다. 기기들은 다음 접속 때 그 버전으로 되돌아간다.

> ⚠ 앱을 새로 빌드할 땐 **반드시 원래 서명 키로** 해야 한다. 키가 다르면 기존 태블릿에
> 설치가 거부되고, 기기마다 사람이 가서 재설치해야 한다. 자세한 건 `원격관리_업데이트.md`.

---

## 4. 영상 관리

### ① 자료실에 넣기 (영상 자료실 탭)

**이 폴더에 파일을 복사해 넣으면 끝이다.**

```
C:\Users\kimdh\Desktop\tablet_kiosk\DobedubKiosk\server\data\videos
```

복사가 **완전히 끝난 뒤** 브라우저를 새로고침하면 목록에 나타난다. 큰 파일은 검증(해시)에
몇 초 걸려서 한 번 더 새로고침해야 보일 수 있다. **폴더에서 지우면 자료실에서도 사라진다.**

원격에서 작업할 때만 "브라우저로 업로드"를 쓴다(수 GB 파일엔 폴더 쪽이 훨씬 빠르다).

### ② 태블릿으로 보내기 (기기 현황 탭)

1. 보낼 기기의 **영상 보내기…** 클릭
2. 목록에서 **여러 개를 체크** → "선택한 영상 보내기"
   - `보유 중` / `전송 대기` 인 항목은 회색으로 선택이 막혀 있다
3. `수신대기 N` 으로 표시되고, 태블릿이 접속하면 내려받는다

용량이 커서(편당 수백 MB) 시간이 걸린다. 파일이 깨지면 기기가 알아서 버리고 다음에 다시
받으므로, **"전송 대기"가 한동안 유지되는 것은 정상**이다. 취소하려면 목록을 펼쳐
"취소"를 누른다(이미 받는 중이면 다음 접속에 반영).

### ③ 태블릿에서 지우기

기기의 영상 목록을 펼쳐 해당 영상의 **삭제**. `삭제 대기` 로 표시되고, 다음 접속 때
기기에서 실제로 지워진다. **자료실 원본은 그대로 남는다.**

반대로 자료실에서 삭제하면 원본이 지워지지만, **이미 태블릿에 내려간 사본은 안 지워진다.**
기기에서도 없애려면 위 ③ 을 따로 해야 한다.

### ④ 썸네일 (태블릿 동영상 목록에 보이는 표지)

영상 자료실 탭에서 각 영상의 **썸네일 등록/교체** → 이미지 선택. 등록하면 그 영상을
가진 태블릿이 다음 접속 때 내려받아 동영상 목록에 표시한다.
등록하지 않으면 태블릿이 **영상 첫 장면**을 자동으로 쓴다.

> 방금 보낸 영상의 썸네일은 **한 박자 늦게** 따라간다(영상 도착 → 그 다음 접속에 썸네일).

---

## 5. 기기별 설정 (기기 현황 탭 → 관리)

### 문의 연락처

문제 발생 시 태블릿 화면에 표시되는 번호. 기본값 `02-334-2227`.
**태블릿에서는 수정할 수 없고 여기서만 바꾼다** — 양쪽에서 고칠 수 있으면 관리자 값이
계속 덮어써서 현장 수정이 되돌아가기 때문이다. 도서관별로 다른 번호를 쓸 때 사용한다.
`적용 대기` 는 아직 기기에 안 닿았다는 뜻이고, 닿으면 자동으로 사라진다.

### PIN 초기화

현장에서 관리자 PIN 을 잊었을 때의 **유일한 복구 수단**. 누르면 다음 접속 때 `0000` 으로
돌아간다. 기기가 "초기화됐다"고 보고할 때까지 `PIN 초기화 대기중` 이 유지되며,
지시가 유실되면 자동으로 다시 보낸다.

### 기기 기록 삭제

목록에서만 지운다. 태블릿은 멀쩡하고, 다시 접속하면 **새 기기처럼 재등록된다.**
반납·폐기한 기기를 목록에서 치울 때 쓴다.

---

## 6. 자주 있는 상황

| 상황 | 확인할 것 |
|---|---|
| 태블릿이 목록에 없다 | 서버 주소 미설정 → §2 "목록에 아예 없을 때" |
| 어제까지 되던 게 오늘 다 빨간색 | **PC 재부팅으로 서버가 꺼진 것.** `관리자-실행.bat` 실행 |
| 노란색(미접속)이 많다 | 정상. 태블릿이 절전 중. 24시간 넘어 빨간색이 되면 그때 확인 |
| 영상을 보냈는데 안 간다 | 그 기기가 접속해야 시작된다. 태블릿에서 "업데이트 확인"을 누르면 즉시 |
| 업데이트가 안 깔린다 | 이용자가 사용 중이면 미룬다. 급하면 "알림 보내기" |
| 자료실에 영상이 안 뜬다 | 복사가 끝났는지 확인 후 새로고침. 큰 파일은 한 번 더 |
| 릴리스 메모가 깨져 나온다 | 명령줄로 올린 것. **대시보드 폼**으로 올릴 것 |
| 다른 도서관 태블릿이 안 붙는다 | 지금은 같은 와이파이만 가능. 외부 접속은 `원격관리_Tailscale_기획.md` 참조(미구현) |

---

## 7. 참고 — 태블릿 쪽 관리자 화면

홈 제목 **5회 탭 → PIN `0000`** (기본값, 변경 가능).

첫 화면에 바로 있는 것: **키오스크 해제 / 키오스크 재진입 / Wi-Fi 설정 / 업데이트 확인**,
맨 아래에 **기기 재부팅 / 키오스크 완전 해제**(확인창).

목록 안: 관리자 정보(PIN 변경·연락처 확인) · 콘텐츠 설정(시작 URL) · 기기 설정(밝기·볼륨) ·
원격 관리(**서버 주소·기관명**) · 정보.

> **키오스크 완전 해제**는 되돌릴 수 없다. 태블릿을 반납하거나 다른 용도로 전환할 때만
> 쓴다. 다시 키오스크로 쓰려면 공장초기화 후 재설정이 필요하다.
