*.iml
.gradle/
/local.properties
.idea/
.DS_Store
/build/
/captures
.externalNativeBuild/
.cxx/
*.apk
*.aab

app/build/
app/release/

# 서명 키 — 절대 커밋 금지(별도 안전 백업). 분실 시 기존 기기 자동 업데이트 불가.
keystore.properties
*.jks
*.keystore

# 서버 런타임(로컬 개발용)
server/node_modules/
server/data/

# Claude Code 머신별 로컬 설정(권한 허용 목록 등)
.claude/
