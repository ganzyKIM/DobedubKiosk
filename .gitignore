*.iml
.gradle/
/local.properties
.idea/
.DS_Store
/build/
/captures
.externalNativeBuild/
.cxx/
*.apk
*.aab

app/build/
app/release/

# 서명 키 — 절대 커밋 금지(별도 안전 백업). 분실 시 기존 기기 자동 업데이트 불가.
keystore.properties
*.jks
*.keystore

# 서버 런타임(로컬 개발용)
server/node_modules/
server/data/


# NetBird 태블릿 등록용 사전 인증 키 — 공개 저장소에 절대 커밋 금지.
# 유출 시 대시보드(Settings > Setup Keys)에서 revoke 하고 새 키로 교체할 것.
netbird-setup-key.txt


# 운영 패키지 조립 산출물(make-package.sh)
dist/
