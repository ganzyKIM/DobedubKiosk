# NetBird 원격 관리 — 실측 QA 가이드

도입(2026-08-15, `원격관리_NetBird_도입.md`) 이후 남은 검증. **테스트 기기 1대(HA1EKRNR) +
맥 + 휴대폰 핫스팟**으로 진행한다. 기존 3대(마스터 PC 함대)는 부재 — 맨 아래 "지금 못 하는
것" 참조.

측정 원칙: USB 를 꽂아두면 태블릿이 어느 와이파이에 있든 adb 측정이 된다(USB 는 네트워크와
무관). 서버 쪽 관찰(체크인 시각·소스 IP·전송 속도)과 `netbird status --detail`(직결/릴레이,
레이턴시)을 근거로 기록한다.

## 진행 방법

각 항목의 **[사람]** 은 손으로 할 일, **[자동]** 은 Claude/스크립트가 서버·adb 로 측정할 일.
결과는 이 문서의 기록표에 추기한다.

---

### QA-1. 다른 망 연결·체크인 ★핵심

도서관 시나리오 재현 — 사무실 LAN 을 벗어나도 고정 주소 하나로 관리되는가.

- **[사람]** 휴대폰 핫스팟 켜기 → 태블릿 관리자(로고 5탭→PIN) → Wi-Fi 설정 → 핫스팟 접속
  (비밀번호는 화면에서 직접 입력) → "붙었다"고 알리기
- **[자동]** ① `netbird status --detail` 로 연결 유형(**P2P direct / relayed**)과 레이턴시
  ② 체크인 도착 + 소스 IP(100.109.230.149 유지 확인) ③ 즉시 깨우기 반응 시간(목표 ≤5초)
- **합격**: 체크인·깨우기 정상. direct 면 최상. **relayed 로 떠도 불합격이 아니다** —
  QA-2 속도가 판단 기준이 된다. 어느 쪽인지 기록이 중요(도서관별 예측 근거).

### QA-2. 다른 망 대용량 영상 push 속도 ★핵심

- **[자동]** QA-1 상태에서 김수환 1화(175MB)를 push → 진행률 보고로 MB/s 실측 →
  1GB 환산 소요 계산 → 끝나면 원격 삭제로 정리
- **판단**: direct ≈ 핫스팟 회선 속도가 정상. relayed 에서 수 Mbps 수준이면 기획문서의
  우려가 현실 — "영상만 외부 스토리지" 우회를 그때 검토(지금은 기록만).

### QA-3. 망 전환 생존 (핸드오버)

- **[사람]** 태블릿을 핫스팟 → 사무실 와이파이로 복귀
- **[자동]** 재연결까지 걸린 시간, 체크인 재개, 깨우기 정상 여부
- **합격**: 개입 없이 수 분 내 복귀. (WireGuard 는 roaming 에 강하다 — 확인 차원)

### QA-4. 재부팅 생존 ★최중요 미검증

NetBird 앱이 재부팅 후 **스스로** VPN 을 복구하는가. 안 되면 도서관 정전 한 번에 원격
관리가 끊긴다.

- **[자동]** `adb reboot` → 부팅 후 관찰: ① 넷버드 IP(100.x) 자동 복귀 여부와 소요 시간
  ② 체크인 재개(부팅 ~60초 후 자동 체크인이 넷버드 주소로 오는지) ③ 깨우기 정상
- **합격**: 개입 없이 복귀.
- **불합격 시 조치 확정**: 앱에 Device Owner `setAlwaysOnVpnPackage("io.netbird.client",
  lockdown=false)` 코드 추가(다음 릴리스). lockdown=false 인 이유는 구 기획문서 승계 —
  VPN 이 죽어도 키오스크 서비스(도서관 웹뷰)는 살아야 한다.
- 참고: 재부팅 후 화면이 가로(rotation 1)로 뜨는 간헐 현상 있음 — `settings put system
  user_rotation 0` (CLAUDE.md v2.3.2 항목).

### QA-5. 밤샘 doze 생존

- **[사람]** 퇴근 전 태블릿을 충전기에 꽂고 화면 끈 채 방치 (USB 는 꽂아두면 좋음)
- **[자동]** 다음날 checkins 기록으로 밤사이 간격 분석 + 아침 넷버드 상태
- **판단 기준**: 체크인 간격이 벌어지는 건 **정상**(doze — 문서화된 절전). 확인할 것은
  ① 아침에 깨우면 복귀하는가 ② 넷버드 세션이 살아 있는가(재로그인 불필요)뿐.

### QA-6 (선택). 세팅 스크립트 5.8 완주 검증

스크립트의 NetBird 등록 흐름은 아직 완주 전(수동 절차만 종주). 신규 기기 없이도 검증 가능:

- **[자동]** ① 대시보드에서 태블릿 피어 삭제 + 앱 데이터 초기화(`pm clear io.netbird.client`)
  ② 키오스크 해제 상태에서 `./setup-tablet.sh --skip-video --skip-webview --skip-debloat`
  ③ 5.8 단계가 등록→100.x 확인까지 완주하는지 → 끝나면 재잠금
- 20분짜리 검증으로 다음 납품의 리스크를 미리 제거할 수 있다.

---

## 기록표 (실측값 추기)

| 일시 | 항목 | 망 | 연결유형 | 결과/측정값 |
|---|---|---|---|---|
| 2026-08-15 | (기준) 같은 LAN | 사무실 Wi-Fi | P2P direct (6ms) | 체크인·깨우기 1초, 정책 통과, Lock Task 공존 |
| 2026-08-15 | QA-1 | 휴대폰 핫스팟 | **Relayed** (icn 릴레이, 12ms) | ✅ 체크인·깨우기 정상(깨우기 3초). 모바일 CGNAT 라 P2P 실패 → 릴레이 폴백. ap_ssid 가 null 로 보고된 것 관찰(추적 필요) |
| 2026-08-15 | QA-2 | 핫스팟(릴레이) | Relayed | **61MB APK / 166초 ≈ 390KB/s(~3.1Mbps)**. 1GB 환산 ~45분. 도서관별 direct/relay 기록 필요성 확인 |
| | QA-3 | | | (미실시) |
| 2026-08-15 | QA-4 | 핫스팟 | — | ❌ **불합격** — 재부팅 후 NetBird 미복귀(피어 Idle), 원격 관리 끊김. **조치: v2.4.1 에 Device Owner always-on VPN(lockdown=false) 추가** → 재시험 예정 |
| 2026-08-15 | QA-4 재시험 | 핫스팟 | — | ⚠ **미복귀·판정 보류** — v2.5 원격 재부팅(22:20:53)으로 재시험. 재부팅 지시 자체는 1초 만에 먹혔으나, 이후 19분 감시에도 NetBird 관리 콘솔 last-seen 이 재부팅 시각에 고정(에이전트가 한 번도 재접속 못 함), 맥 쪽 피어 상태 Connecting→**Idle** 로 하강 — 최초 QA-4 불합격과 동일한 시그니처. 원격에서는 ①핫스팟이 클라이언트 0 이 되며 꺼짐(환경) ②always-on VPN 미작동(코드) 을 구분할 수 없다 — 둘 다 증상이 동일(체크인·관리콘솔 모두 무소식). **실물 확인 필요**: 폰 핫스팟이 켜져 있는지 → 켜져 있는데 태블릿만 안 붙으면 always-on 실패이므로 태블릿에서 NetBird 상태 확인. 후속 개선 후보: 체크인 payload 에 `dpm.getAlwaysOnVpnPackage` 값을 실어 always-on 지정이 실제로 걸렸는지 대시보드에서 보이게 |
| | QA-5 | | | |
| | QA-6 | — | — | |

**v2.5 원격 대응 기능 E2E (2026-08-15 밤, 핫스팟·릴레이 경유)**
- 강제 업데이트 v2.4.1→v2.5: 지시 발사 22:17:23 → 깨우기·체크인 수 초 → 64MB 다운로드
  → 설치 → 재시작 → **code 27 체크인 22:19:02 (총 105초)**. force_update 자동 해제,
  기기 fleetUrl 보고 확인. 이번엔 설치 후 재기동 걸림(검은 화면) **재발 안 함**.
- 서버 주소 원격 변경 안전장치: 도달 불가 주소(192.0.2.1) 지시 → 기기가 /health probe
  실패로 **저장 거부, 기존 주소 유지**(22:19:37 체크인으로 확인). override 는 "변경 대기"
  로 남음(=실패 신호) → 빈 값으로 회수 정상.
- 원격 재부팅: 발사 22:20:53 → **1초 만에 지시 수신**(22:20:54 체크인) → 10초 내 연결
  소멸(=dpm.reboot 실행). 1회성 규약(플래그 즉시 소거) 동작 확인.

**QA 중 관찰된 부수 사실**
- v2.4 원격 강제 업데이트: 릴레이 경로로 다운로드·검증·설치 커밋까지 완주했으나 설치 후
  재기동 걸림(기지 간헐 이슈, CLAUDE.md v2.3.2 항목) 재발 — 이번엔 adb 가 없어 사용자
  전원 재시작으로 복구. **원격 재부팅 지시**가 있으면 좋겠다는 필요성 확인(후보 기능).
- 릴레이 경로에서도 즉시 깨우기 3초, 진행률 보고 정상.

## 지금 못 하는 것 (기존 3대 부재)

- **기존 함대 이전**: 마스터 PC 서버에 NetBird 설치 + `fleet-server` 그룹 배정, 기존 3대에
  NetBird 등록·주소 전환. 기기가 손에 들어오거나, `setFleetUrl` 원격 지시가 개발되면
  마스터 PC 함대 쪽에서 원격으로 진행 가능.
- **`setFleetUrl` 자체는 기기 없이도 개발 가능** — 서버+앱 구현 후 이 태블릿(HA1EKRNR)으로
  검증하면 된다. (태스크 #9)
- 규모(수십 대) 동시 체크인/전송 부하 — 납품 물량 확보 후.
